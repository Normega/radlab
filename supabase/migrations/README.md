# Migration manifest — applied status

> **All migration files in this directory are applied to the live Supabase project**, verified
> 2026-07-08 by cross-checking `supabase_migrations.schema_migrations` and, for files with no
> history record, by confirming their effects (tables/columns/functions/triggers/policies/seed
> rows) exist in the live database. **Future sessions: trust this manifest — only migrations
> dated after 2026-07-08 need checking.** When you apply a new migration, add it to the table
> below with its evidence.

## How to read this

Migrations were applied through a mix of the Supabase SQL editor (manual — leaves **no** history
record) and the MCP `apply_migration` tool (records into `supabase_migrations.schema_migrations`,
often under a **different name** than the file). So the history table alone is not the ground
truth — the live schema is. Evidence below is either `history: <recorded name>` or
`live: <object(s) confirmed 2026-07-08>`.

## Dated migrations

| File | Applied — evidence |
|---|---|
| `20260510_study_infrastructure.sql` | live: `activities`, `session_templates`, `session_template_nodes`, `study_protocols` family, `participant_schedule`, `participant_links`, `message_log`, `participant_activity_log` all exist. (`participant_consent` was created here then deliberately dropped by `20260602_study_admin_redesign` — its absence is correct.) |
| `20260512_get_user_by_email.sql` | live: `get_user_id_by_email/1` (later hardened by `20260611_get_user_by_email_lab_only`) |
| `20260512_super_admin.sql` | live: `profiles.super_admin` |
| `20260513_message_log_test_flag.sql` | live: `message_log.is_test` |
| `20260515_protocol_email_fields.sql` | history: `protocol_email_fields` |
| `20260515_unsubscribe_tokens.sql` | history: `unsubscribe_tokens` |
| `20260525_activities_questionnaire_nodes.sql` | live: `session_template_nodes.questionnaire_id` |
| `20260525_debrief_forms.sql` | live: `study_debrief_forms` table, `studies.active_debrief_form_id` (history follow-ups: `fix_debrief_forms_rls_policy`, `add_debrief_forms_storage_policy`) |
| `20260526_videos_bucket_storage_policies.sql` | history: `videos_bucket_storage_policies` |
| `20260529_belt_trigger_device.sql` | live: `belt_sessions.trigger_device` |
| `20260602_session_entry_rpc.sql` | live: `get_session_by_token/1`, `complete_session_by_token/1` (function body since superseded by `20260623_screener.sql`) |
| `20260602_study_admin_redesign.sql` | live: `studies.delivery_mode`, `study_sessions`, rebuilt `participant_schedule`/`participant_links`, `study_enrollments`; `participant_consent` dropped |
| `20260603_belt_participant_external_id.sql` | history: `belt_participant_external_id` |
| `20260604_activities_demographics.sql` | history: `activities_demographics` |
| `20260604_belt_sessions_storage_lab_policy.sql` | history: `belt_sessions_storage_lab_policy` |
| `20260604_belt_timing_fields.sql` | history: `belt_timing_fields` |
| `20260604_belt_trial_onset_and_thresholds.sql` | history: `belt_trial_onset_and_thresholds` |
| `20260604_cascade_study_session_delete.sql` | history: `cascade_study_session_delete` |
| `20260604_demographics.sql` | history: `demographics` |
| `20260604_game_sessions_lab_policy.sql` | history: `game_sessions_lab_policy` |
| `20260604_lab_write_policies_all_game_tables.sql` | history: `lab_write_policies_all_game_tables` |
| `20260604_lab_write_policies_performance_trials.sql` | history: `lab_write_policies_performance_trials` |
| `20260604_session_nodes_questionnaires.sql` | live: `get_session_by_token` returns questionnaire nodes (body since superseded by `20260623_screener.sql`) |
| `20260606_activities_physio_belt_setup.sql` | history: `20260606_activities_add_physio_category`; live: `activities` row `physio/belt_setup` |
| `20260606_activities_unique_category_subcategory.sql` | history: `20260606_activities_unique_category_subcategory` |
| `20260606_compensation_form.sql` | live: `participant_compensation` table, `activities` row `form/compensation` |
| `20260609_intervention_responses_day_fk.sql` | history: `intervention_responses_day_data_fk` |
| `20260609_lexical_sessions.sql` | history: `word_max_sessions`; live: `word_max_sessions` table |
| `20260609_training_infrastructure.sql` | history: `training_infrastructure` |
| `20260609_video_library.sql` | history: `video_library` |
| `20260610_audio_rls_fix.sql` | history: `audio_fix_rls_lab_role` |
| `20260610_audio_schema.sql` | live: `study_audios`, `participant_audio_sessions`, `participant_audio_events`, `complete_audio_session/3` (history follow-up: `audio_add_folder_and_file_meta`) |
| `20260610_cascade_game_and_liliana_fks.sql` | history: `cascade_game_and_liliana_fks` |
| `20260610_colormax.sql` | history: `colormax_schema` |
| `20260610_enrollment_delete_cascade.sql` | history: `enrollment_delete_cascade` |
| `20260610_set_null_creator_and_self_ref_fks.sql` | history: `set_null_creator_and_self_ref_fks` |
| `20260610_study_delete_cascade.sql` | history: `study_delete_cascade` |
| `20260611_audios_storage_bucket.sql` | history: `audios_storage_bucket` |
| `20260611_cleanup_admin_role_policies.sql` | history: `cleanup_admin_role_policies` |
| `20260611_get_user_by_email_lab_only.sql` | history: `get_user_by_email_lab_only` |
| `20260611_medium_security_fixes.sql` | history: `medium_security_fixes` |
| `20260611_session_token_expiry_enforcement.sql` | history: `session_token_expiry_enforcement` |
| `20260611_super_admin_profiles_lockdown.sql` | history: `super_admin_profiles_lockdown` |
| `20260616_activities_category_add_vas.sql` | history: `activities_category_add_vas_training` |
| `20260616_public_assets_bucket.sql` | history: `public_assets_bucket` |
| `20260616_vas_packages.sql` | history: `vas_packages` |
| `20260616_vas_scales.sql` | history: `vas_scales` |
| `20260616_word_max_color_max_activities.sql` | history: `word_max_color_max_activities` |
| `20260617_external_enrollment.sql` | history: `20260617_external_enrollment` |
| `20260617_fix_audio_storage_paths.sql` | data-only fix (storage path strings); audio library functional since — no independent schema object to verify |
| `20260617_slider_scales.sql` | history: `slider_scales` |
| `20260618_profiles_study_id_guard.sql` | live: `my_study_id/0`, policy `profiles: own update safe` |
| `20260618_vas_activities_four_scales.sql` | live: `activities` rows `vas/vas_sleep`, `vas_stress`, `vas_enjoyment`, `vas_helpful`, `vas_effort` |
| `20260618_vas_emoji_url_placeholder_fix.sql` | live: zero `vas_scales` rows with placeholder anchor URLs |
| `20260619_vas_confidence_satisfaction_activities.sql` | live: `activities` rows `vas/vas_confidence`, `vas_task-satisfaction`, `vas_life-satisfaction` |
| `20260619_vas_confidence_satisfaction_seed.sql` | live: `vas_scales` rows `confidence`, `task-satisfaction`, `life-satisfaction` |
| `20260623_screener.sql` | live: `screener_results` + own-rows policy, `studies.screener`/`screener_id`, current `get_session_by_token` |
| `20260623_screeners_table.sql` | live: `screeners` table, policy `lab manage screeners`, seed row `emotion-regulation-v1` |
| `20260623_vas_packages_items.sql` | live: `vas_packages.items` |
| `20260624_experiment_builder.sql` | history: `experiment_builder` (recorded 2026-06-25) |
| `20260705_assignment_randomizer.sql` | live: `draw_assignment`, `studies.assignment_slots`, `assignment_balance` view; pilot-verified 2026-07-05 (website.md §28) |
| `20260705_displays.sql` | history: `20260705_displays` |
| `20260705_session_token_assignment_slots.sql` | history: `20260705_session_token_assignment_slots` |
| `20260707_activities_category_add_display.sql` | history: `activities_category_add_display` |
| `20260707_profiles_prevent_privilege_escalation.sql` | live: `prevent_self_privilege_escalation/0`, trigger `profiles_prevent_privesc` |
| `20260708_phase2_draw_assignment.sql` | live: `draw_assignment/3` (3-arg service-role variant); verified live 2026-07-08 (website.md §28 Phase 2 Pass 1) |
| `20260708_vas_schedule_linkage.sql` | history: `vas_schedule_linkage`; live: `vas_responses.schedule_id`/`package_slug`, `liliana_day_data.module_id` |
| `20260709_liliana_feedback_backend.sql` | history: `liliana_feedback_backend` (+ three follow-up statements applied directly and folded into the file: schedule FK → ON DELETE SET NULL, kind CHECK gains 'choice'); live: `liliana_session_metrics` view, `liliana_midpoint_feedback` table, `get_liliana_midpoint_summary/1`, `record_practice_decision/3`, patched `draw_assignment/3` — full synthetic-data verification 2026-07-09 |
| `20260709_liliana_midpoint_choice_rework.sql` | history: `liliana_midpoint_choice_rework`; live: `liliana_midpoint_feedback.stated_preference`, `phase2_source` CHECK ('choice','anti_preference'), `participant_assignments.kind` gains 'anti_preference', reworked `record_practice_decision` (node auto-detect + server-side anti-preference 50/50), activities category CHECK gains 'midpoint' + `midpoint/liliana_midpoint` row — synthetic-data verification 2026-07-09 |
| `20260709_ensure_liliana_participant.sql` | history: `ensure_liliana_participant`; live: `ensure_liliana_participant/1` — exercised via real participant link 2026-07-09 |
| `20260709_session_token_training_nodes.sql` | history: `session_token_training_nodes`; live: `get_session_by_token` returns `module_id` + synthesized training activities — exercised via real participant link 2026-07-09 |
| `20260710_metric_v2_primary.sql` | history: `metric_v2_primary`; live: `get_liliana_midpoint_summary` ranks by mean Δstress (metric v2, per methods doc), v1 retained in snapshots — designed-disagreement verification 2026-07-10 |
| `20260710_preference_ranking.sql` | history: `preference_ranking`; live: `liliana_midpoint_feedback.preference_ranking`, `record_practice_decision/4` (ranking validation + stated_preference = rank #1) — browser-verified 2026-07-14 |
| `20260710_schedule_last_sent_at.sql` | history: `schedule_last_sent_at`; live: `participant_schedule.last_sent_at` (reminder cadence anchor for check_schedule v10) |
| `20260714_video_step.sql` | history: `video_step`; live: activities category CHECK gains 'video', `video/049fd6a4…` intro-video row, Baseline template node at order 12 |
| `20260714_assessment_leadin_step.sql` | history: `assessment_leadin_step`; live: activities category CHECK gains 'assessment_leadin', `assessment_leadin/midpoint` + `assessment_leadin/post` rows, Liliana Midpoint (`cc839191…`) and Final Assessment (`3d1a1593…`) session templates each gain a node 0 lead-in with existing nodes shifted +1 — verified via query 2026-07-14 |
| `20260714_daily_welcome_farewell.sql` | history: `daily_welcome_farewell`; live: activities category CHECK gains 'daily_welcome'/'daily_farewell', `daily_welcome/liliana` + 3 `daily_farewell/{condition}` rows, all 48 Liliana daily (Phase 1 + Phase 2) session templates gain a Welcome node 0 and a Farewell node at the end, condition-matched via each template's training node — verified via query 2026-07-14 (48/48 templates, both counts) |
| `20260714_final_assessment_questionnaire_fix.sql` | data-only fix; live: `session_template_nodes.questionnaire_id` populated for the 9 previously-NULL nodes (BIPS, GAD-7, PHQ-8, SPANE, PWB, ERQ, MPoD-t, SRI, SSCS-L) on "Liliana Study 3 - Final Assessment" — verified via query 2026-07-14 (all 9 now resolve to real, non-empty questionnaires; study-wide sweep for `activity_id`/`questionnaire_id`/`module_id` all NULL returns zero rows) |
| `20260714_liliana_consent_debrief_screener.sql` | data-only fix; live: dry-run study (`dddddddd-…`) gains `screener_id`/`screener` (existing `emotion-regulation-v1` screener, plus a new `description` block added to `screeners.definition`), a new `study_consent_forms` row wired to `active_consent_form_id`, and a new `study_debrief_forms` row wired to `active_debrief_form_id` — content converted via `mammoth` from `Ethics/Informed Consent Form_LW.docx` / `Debriefing Form_LW.docx` — verified via query 2026-07-14 (all three fields populated, `screener` jsonb has all 8 expected top-level keys including the new `description`) |
| `20260714_liliana_sona_credit_report.sql` | live: `get_liliana_credit_report(p_study_id)` function — verified live 2026-07-14 against the dry-run study's 5 real WP-L5 participants (correct `total_minutes`/`credit_hours` math, correct `sona_identifier`/`external_source` resolution from `study_enrollments.external_id`) |
| `20260710_lecture_lounge_schema.sql` | history: `lecture_lounge_schema`; live: `classes`, `class_admins`, `class_members`, `lectures`, `checkins`, `checkin_responses`, `class_questions`, `question_votes` (all RLS-enabled, all with policies), `checkin_responses` added to `supabase_realtime` publication, `verify_class_email/1`, `award_checkin_points/1`, `get_checkin_mood_results/1` — full RLS verified live 2026-07-10 by impersonating a lab admin, a per-class admin, a member, and a non-member via `request.jwt.claim.sub` (own-row join, admin-gated lecture/checkin writes, open-only response writes, cross-member read isolation on `checkin_responses`, anonymized RPC results, idempotent points award, post-close write lock); test rows created and cascade-deleted, table row counts confirmed zero after cleanup |
| `20260710_lecture_lounge_email_verify_lockdown.sql` | history: `lecture_lounge_email_verify_lockdown`; live: tightened `class_members: own update pre-verify` WITH CHECK (client update can no longer leave a non-null `email_verify_token`/`email_verify_expires_at`), `verify_class_email/1` now also returns the class `slug`. Caught during WP2 design: the original policy let a client set its own verification token via direct UPDATE and self-verify without ever receiving the email — reproduced live pre-fix, confirmed rejected post-fix |
| `20260711_lecture_lounge_lab_admin_parity.sql` | history: `lecture_lounge_lab_admin_parity`; live: `lectures: admins all`, `checkins: admins all`, `checkin_responses: admins read all`, `class_questions: admins read all`/`admins update`, `get_checkin_mood_results/1` all widened to accept `my_role() = 'lab' OR is_super_admin()` alongside `class_admins` membership, matching the `classes: admins update` policy from the WP1 migration. Caught live 2026-07-11: Norm's lab-role account passed `ClassAdminRoute`'s UI gate (which already allowed lab/super_admin) but every write on `lectures` silently failed RLS since that policy only ever checked `class_admins` — reproduced pre-fix (`42501`), confirmed fixed post-fix, non-admin still correctly rejected |
| `20260711_list_class_admins.sql` | history: `list_class_admins`; live: `list_class_admins/1` (lab-only SECURITY DEFINER, joins `class_admins` to `auth.users` for email display — `profiles` carries no email column). Backs the new `/admin/classes` screen (create class, add/remove instructor by email via the existing `get_user_id_by_email`) closing the gap that Phase 1 had no UI for either action. Verified live end-to-end: create class → email lookup → add admin → list shows correct email → cascade-delete cleans up |
| `20260711_utoronto_verification_account_level.sql` | history: `utoronto_verification_account_level`; live: `utoronto_email`/`utoronto_verified_at`/`email_verify_token`/`email_verify_expires_at` moved from `class_members` to `profiles` (verification is per-account now, not per-class-membership — proving ownership once carries to every class joined afterward), protected by a new `protect_utoronto_verification` BEFORE UPDATE trigger (any write arriving as the `authenticated` role has these 4 columns forced back to their old values — RLS's broad `profiles: own update safe` policy can't do column-level restriction without breaking every other profile edit, e.g. avatar/points, so this uses a trigger instead; SECURITY DEFINER functions and the `service_role` Edge Function both run as a different `current_user` and bypass it). `verify_class_email` replaced by `verify_utoronto_email/1` (no longer resolves a class slug — the initiating class's slug now travels as its own query param on the emailed link instead). Verified live: raw client UPDATE attempt to spoof `utoronto_verified_at` silently reverted by the trigger; legitimate RPC path sets it correctly; cross-class carryover confirmed (verify via class A, `profiles.utoronto_verified_at` reads true independent of which class's membership row is queried) |
| `20260711_lecture_lounge_remote_guards.sql` | history: `lecture_lounge_remote_guards`; live: `enforce_single_live_checkin` BEFORE UPDATE trigger on `checkins` (DB-level, not UI-only — rejects setting a checkin to `staged`/`open` while another checkin in the same class already is), and `checkin_responses: own write/update while open` widened to independently check the auto-close deadline (`opened_at + auto_close_seconds`) so a late submission is rejected even if the client-side countdown never got the chance to flip `checkins.status` to `closed` itself. Both per the WP3b brief's explicit language ("guard on state transitions", "not client timers alone"). Verified live: opening a second checkin while one is already open in the same class raises `P0001` and is rejected |
| `20260712_checkin_mood_results_emotion_zone.sql` | history: `checkin_mood_results_emotion_zone` (superseded a same-session `checkin_mood_results_drop_unused_neutral_field` iteration, folded into this file before commit); live: `get_checkin_mood_results/1` now also returns `emotion_id`/`zone` (previously only `valence`/`arousal`). Fixes a real display bug: `WheelSVG` draws each emotion's wedge at a fixed angular slot that doesn't correspond to that emotion's actual stored valence/arousal (confirmed numerically — e.g. Alert's wedge points straight up, its valence/arousal would plot up-and-left), so `ResultsView` was plotting dots in the wrong wedge relative to the background grid. Frontend now positions dots via the wedge's own angle+zone geometry instead. Verified live: RPC returns `emotion_id`/`zone` correctly for a real response; `jsonb ->> 'emotion_id'` on a neutral tap's `{"emotion_id":null,...}` confirmed returns true SQL NULL (not the string `"null"`), matching the frontend's `emotion_id == null` neutral check |
| `20260712_class_participation.sql` | history: `class_participation`; live: `get_class_participation/1` (lab/super_admin/that class's `class_admins` only) — returns members (with `profiles.utoronto_email`/`utoronto_verified_at`), lectures, and per-(member, lecture) response counts for the Phase 2 participation matrix + CSV export, per website.md §29's original design. Needed since no `profiles` RLS policy lets a non-lab class admin read another student's `utoronto_email` — same narrow-RPC pattern as `list_class_admins`/`get_checkin_mood_results` rather than a broader policy that would also expose unrelated profile data. Verified live against real data on class `n2`: correct member/lecture/count shape for an authorized class admin, `forbidden` for an outsider |
| `20260712_question_lifecycle.sql` | history: `question_lifecycle`; live: `question_votes: members read` widened to accept `my_role() = 'lab' OR is_super_admin() OR class_admins` alongside `class_members` — the brief requires the instructor's live question feed to sort published questions by upvotes, but the original WP1 policy only let actual class *members* (students) read vote counts, missing the same lab-admin-parity gap already fixed once for lectures/checkins/class_questions (this table was overlooked then since no UI needed it yet). `class_questions` added to the `supabase_realtime` publication for the remote's live feed. Verified live: a non-lab `class_admins` account (not also a `class_members` row) can now read a vote row that previously returned empty under RLS |
| `20260712_admin_user_management.sql` | live: `admin_list_users`, `admin_set_user_role`, `admin_delete_user` confirmed working 2026-07-12 — delete was callable (returned FK error, not "function not found"), confirming the RPC exists; role toggle also functional per admin UI |
| `20260712_ripple_wp1.sql` | live: `ripples`, `ripple_checkins`, `consents` all confirmed in `information_schema.tables` 2026-07-12 |
| `20260712_admin_delete_cascade.sql` | live: `admin_delete_user` replaced with comprehensive version that explicitly deletes all user-owned data tables before profiles (demographics, ripples, consents, belt/face_read/farm_joy/drift/stillwater/vas game data, lecture lounge rows, avatars) — applied 2026-07-12 via MCP after FK constraint blocked deletion of a test user who had no demographics row from incomplete onboarding |
| `20260713_lecture_lounge_quiz.sql` | history: `lecture_lounge_quiz`; live: `checkin_responses.quiz_answers` (jsonb), `checkins.quiz_revealed_at` (timestamptz), new `checkin_quiz_keys` table (PK+FK `checkin_id`, `answer_key` jsonb) with an admins-only RLS policy and deliberately **no student-facing policy at all** — correct answers can't live in `checkins.config` (students already read that column directly today) or any row students can SELECT, so they get their own table with zero direct student access. `get_checkin_quiz_results/1` (SECURITY DEFINER) is the only path to them: always returns `items`/server-aggregated `counts` (safe), but `answer_key` only once `quiz_revealed_at IS NOT NULL` or the caller is an admin. `checkins` added to the `supabase_realtime` publication so `ResultsView` can pick up a reveal live via `postgres_changes` without a new broadcast event. Verified live end-to-end on class `n2` with real accounts (a non-lab `class_admins` account as instructor, a real student profile added/removed from `class_members` for the test): instructor created a checkin + `checkin_quiz_keys` row, student submitted `quiz_answers`, RPC returned counts with `answer_key: null` and `revealed: false` pre-reveal (confirmed direct `SELECT * FROM checkin_quiz_keys` as the student returns zero rows), instructor closed → results_ready → set `quiz_revealed_at`, RPC then returned the real `answer_key` to the student. All test rows cascade-deleted afterward, confirmed via count queries |
| `20260713_checkin_dismiss.sql` | history: `checkin_dismiss`; live: `checkins.dismissed_at` (timestamptz). Found live testing the avatar wall feature: `ClassRoom`/`ClassScreen` restore their state on load by picking the most-recently-touched non-planned checkin with no time cutoff, so a checkin left in `results_ready` (nothing else ever resets it) restores as "the live one" on every reload forever — the true idle/lobby view, and the avatar wall which only renders there, becomes unreachable after the first check-in of a term. No RLS change needed (the existing `checkins: admins all` whole-row policy already covers writing this column). `ClassRemote` gained a "Back to lobby" button on `results_ready` checkins that sets `dismissed_at` and broadcasts a new `dismissed` event (not a checkin status — `ClassRoom`/`ClassScreen` treat it as a distinct signal that goes straight to `liveCheckin = null`); the restore queries in both now filter `.is('dismissed_at', null)`. Verified live on class `n3`: found 3 real stale `results_ready` checkins from Norm's own earlier "testing it out" session blocking the lobby view exactly as described above; dismissed all three, confirmed the restore query then returns zero rows for that class |
| `20260714_ripple_settings.sql` | live: `ripples.check_in_enabled boolean NOT NULL DEFAULT true` — applied 2026-07-14 via Supabase SQL editor; confirmed by user |
| `20260714_ripple_wp4_intentions.sql` | live: `ripple_checkins.intention`, `ripple_checkins.prev_intention_outcome`, `ripples.prompt_cadence` — applied 2026-07-14 via Supabase SQL editor; confirmed by user |
| `20260714_ripple_reminders.sql` | live: `ripples.reminder_enabled`, `ripples.reminder_time`, `ripples.last_reminder_sent_on`, `ripple_unsubscribe_tokens` table — applied 2026-07-14 via Supabase SQL editor; confirmed by user. pg_cron job `ripple-reminders-hourly` (schedule 2) registered same day. |
| `20260713_session_template_folder.sql` | live: `session_templates.folder text` (nullable) — applied 2026-07-13 via MCP; verified via `information_schema.columns` |
| `20260713_equity_census.sql` | history: `equity_census`; live: `equity_census_responses` table (RLS enabled, 3 policies mirroring `demographics`: own all / lab read / lab insert) + `activities` row `form/equity_census` — applied via MCP 2026-07-13 at merge of the equity-census branch; verified live same day (table in `information_schema.tables`, `pg_policy` count 3, `relrowsecurity` true, activities row present) |
| `20260715_pond_watch_results.sql` | history: `20260715_pond_watch_results`; live: `pond_watch_results` table (RLS enabled, 2 policies — `own rows` user_id=auth.uid() / `pond_watch_results: lab write` my_role()='lab', mirroring `drift_performance`) — applied via MCP 2026-07-15 (Zerin Langerian Mindfulness study); verified `relrowsecurity` true + `pg_policy` count 2 same day. Backs PondWatch wired as a game step (`GameStepWrapper` map + `scheduleId` threaded from `StepDispatcher`). |
| `20260715_zerin_daily_checkins.sql` | history: `20260715_zerin_daily_checkins`; live: `zerin_daily_checkins` table (RLS enabled, 2 policies own-rows/lab-write) — one cross-arm daily-touchpoint log for the Zerin study (control/self_monitoring/reflective, discriminated by `arm`). Also seeds 3 `activities` rows (`form/mood_checkin`, `form/mood_checkin_reflective`, `form/wellness_tip`) and **replaces `get_session_by_token`** to add `study_day` + `send_time` to the returned `schedule` object (so the check-in/tip step widgets resolve day + time slot). Applied via MCP 2026-07-15; verified table RLS + activities rows present same day. |
| `20260715_screener_phase1_answers.sql` | history: `20260715_screener_phase1_answers`; live: `screener_results.phase1_answers jsonb` (nullable, additive) — records raw phase-1 eligibility answers so a Zerin distress-gate "unsure" (passes through for follow-up) is recoverable. Applied via MCP 2026-07-15. Benefits Liliana's screener too (same write path now populates it). |
| `20260715_zerin_screener.sql` | history: `20260715_zerin_screener`; live: `screeners` row `langerian-mindfulness-v1` (single-instrument PHQ-8 range gate 5-9, U of T + PSY100 phase-1 gate + distress safety item). Applied via MCP 2026-07-15; verified 8 phase-1 items, phase2 scoring mode 'range', 1 phase-2 questionnaire. Consumed by the generalized `ScreenerPage`/`screenerUtils.evaluateScreenerPhase2` (Liliana's `emotion-regulation-v1` path proven byte-identical via a 56-assertion standalone test). |
| `20260714_adherence_check_schema.sql` | live: `study_enrollments.withdrawal_reason`/`withdrawn_at` + CHECK on `status` ('enrolled'/'in_progress'/'completed'/'withdrawn'), `message_log.kind` (NOT NULL default `'session_link'`, CHECK adds `'adherence_termination'`) — applied via MCP 2026-07-15. Confirmed against live data first: existing `study_enrollments.status` values were only `completed`/`withdrawn`/`enrolled`, so the new CHECK is non-breaking. |
| `20260714_liliana_adherence_check_nodes.sql` | data-only fix; live: dry-run study (`dddddddd-…`) `design_graph` gains two `adherence_check` nodes (`ac_p1` phase1, `ac_p2` phase2, both `min_required:10`/`of_total:12`), spliced between `cb_p1`→`t_mid` and the three Phase 2 condition blocks→`t_final`. Applied via MCP 2026-07-15 after a dry-run SELECT; verified nodes/edges present and, separately, that all 5 real dry-run participants (Phase 1 day-counts 11-12) sit safely above the new gate's threshold. Backs the >=10-of-12 adherence enforcement feature (`materializeSchedule.ts`'s `adherence_check` node type + `processAdherenceWithdrawal.ts`) — full synthetic end-to-end test 2026-07-15 against a real invocation of the deployed `check_schedule` function (8/12-completed test participant correctly withdrawn, reason logged, link revoked, `message_log` row written, re-run confirmed idempotent, test data fully cleaned up). |
| `20260715_duplicate_study.sql` | live: `duplicate_study(p_study_id, p_new_name)` function — applied via MCP 2026-07-15 (two iterations: first attempt hit an FK-ordering bug — cloned consent/debrief forms before the new `studies` row existed — fixed by inserting `studies` first with null form pointers, then cloning forms, then back-filling). Used live to clone "Liliana Study 3 — DRY RUN (WP-L5)" into "Liliana Study 3 — Live Test" (`bf080651-…`): verified `design_graph`/`assignment_slots`/`screener` byte-identical, 51/51 `study_sessions` cloned pointing at the same shared `session_template_id`s, consent/debrief forms deep-cloned with matching `html_content` and re-pointed `active_consent_form_id`/`active_debrief_form_id`, zero participant rows (`study_enrollments`/`participant_schedule`/`participant_links`/`participant_assignments` all 0). Also browser-verified end-to-end through the new `/admin/studies` "Duplicate" UI action (dialog → RPC call → navigate to new study's detail page with sessions rendered) using a throwaway lab-role test account, cleaned up after. |
| `20260715_record_consent.sql` | live: `record_consent(p_study_id)` function — applied via MCP 2026-07-15. Fixes `ConsentPage.jsx`/`ConsentGate.jsx`, which had queried/inserted into `participant_consents`, a table dropped by `20260602_study_admin_redesign.sql` two weeks after the page was written and never updated — found live testing the new "Liliana Study 3 — Live Test" study. Real record of consent is `study_enrollments.consent_date`; the RPC is a narrow SECURITY DEFINER write (mirrors `ensure_liliana_participant`) rather than an RLS UPDATE policy, since `study_enrollments` carries fields (`status`, `withdrawal_reason`, ...) a participant must never self-write. |
| `20260715_consent_debrief_forms_participant_read.sql` | live: new participant-scoped SELECT policies on `study_consent_forms`/`study_debrief_forms` (previously ONLY `lab full access` — no participant, ever, could read their own study's consent or debrief form content). Found live in the same debugging pass: even after fixing `record_consent`, loading the form itself still 403'd under RLS. Scoped to `EXISTS (study_enrollments WHERE profile_id = auth.uid())` for that form's `study_id`, mirroring the existing `study_enrollments: own read` pattern. |
| `20260715_remove_redundant_consent_step.sql` | data-only fix; live: deletes the redundant in-session "consent" step (`session_template_nodes` rows with `activities.category='form'/subcategory='consent'` → `ConsentStep.jsx`, whose result was never persisted anywhere) from all 4 templates that had it (Liliana Study 3 - Baseline, Sandy Study 3, SummerBelt2026_Session, Zerin Baseline), plus the now-unused `activities` row itself. Fully redundant since `ConsentGate` properly gates + persists real consent before a participant ever reaches session steps — this let a participant see the same consent text twice. `order_index` gaps left in place; confirmed no code anywhere requires contiguity (`ORDER BY order_index` throughout, and `SessionBuilder.jsx` renumbers sequentially on next save regardless). `ConsentStep.jsx` deleted, `StepDispatcher.jsx`'s dispatch case removed, `advancedInstruments.js`'s registry entry removed. Verified via direct query post-migration: Liliana's Baseline template's first node is now `order_index 1, form/demographics` (previously `order_index 0` was the removed consent step). |
| `20260715_draw_assignment_gap_safe_index.sql` | live: `draw_assignment` patched to compute the next `draw_index` via `COALESCE(MAX(draw_index), -1) + 1` instead of `COUNT(*)`. Found live the same day: cleaning up a synthetic test participant on "Liliana Study 3 — Live Test" deleted their `participant_assignments` row for the `cb_p1` counterbalance, leaving a gap (indices 0,2,3 — 1 missing). The next real `auto-enroll` attempt's `COUNT(*)`-based computation collided with the existing index-3 row and 500'd ("Failed to schedule this study for the participant") — and would have collided identically on every subsequent attempt forever, permanently blocking new enrollment on that slot. `MAX+1` is identical to `COUNT(*)` in the normal dense case (no behavior change for any existing study) but is gap-safe going forward — verified live: a fresh `auto-enroll` call correctly drew index 4 (skipping the permanent gap at 1) with no collision. |
| `20260716_participant_contact_email.sql` | live: applied via MCP `apply_migration` 2026-07-15. Confirmed post-apply: `study_enrollments.contact_email`/`contact_email_set_at` columns exist, `record_contact_email/2` exists, and `get_session_by_token/1` now emits `study.longitudinal` + `enrollment.external_source`/`contact_email` (its pre-existing base body verified byte-identical to the live definition before applying, so the only change is the three added output fields; smoke-tested with a bogus token → `not_found`). Redeployed all three edge functions the same day so the recipient resolver is live: `send_message` v8 (MCP deploy), `check_schedule` v14 and `auto-enroll` v9 (CLI deploy from disk, both `--no-verify-jwt` to preserve their `verify_jwt=false`; upload manifest shows the new `_shared/participantEmail.ts` + updated `_shared/processAdherenceWithdrawal.ts` bundled into both). Frontend ContactEmailGate ships on merge to main. |
| `20260715_fix_participant_video_sessions_fk.sql` | live: `participant_video_sessions_video_id_fkey` repointed from `study_videos(id)` (dead legacy table, 0 rows, referenced nowhere else live) to `video_library(id)` (the actual registry the video-step feature was built against — confirmed only one FK anywhere referenced `study_videos`, this one). Found live 2026-07-15 by Norm testing Liliana's Baseline session's final step (Introduction Video): every insert into `participant_video_sessions` violated the old FK 100% of the time (deterministic, not a race — `study_videos` is simply empty), surfacing as a blank video window and a `409 Conflict` in the console (PostgREST maps `23503` FK violations to HTTP 409, same code as a unique violation, which is what made it look like a duplicate-insert race at first). Verified via a direct insert against the real video id (`049fd6a4-…`, `Liliana_Study3_Intro`) — succeeds post-fix, cleaned up after. Pure schema fix, no code/redeploy needed — live immediately. |
| `20260716_bfi2_instruction_text.sql` | data-only fix; live: applied via MCP `apply_migration` 2026-07-16. Reworded `questionnaires.definition->>'instructions'` for both `bfi-2-xs` and `bfi-2-s` from "Please write a number next to each statement to indicate" to "Please select a number to indicate" — the platform presents a clickable number scale, not a free-text field, so the old wording was inaccurate ("write a number") for any participant. Verified post-apply by re-selecting both rows' `instructions` field. |
| `20260717_studies_enrolled_participant_read.sql` | live: applied via MCP `apply_migration` (`studies_enrolled_participant_read`) 2026-07-17. New SELECT policy `studies: enrolled participant read` (`EXISTS (study_enrollments WHERE profile_id = auth.uid() AND study_id = studies.id)`). Before this, the only participant-facing read on `studies` was `participant read own` (`profiles.study_id = studies.id`) — set by `auto-enroll` but never by `create_anonymous_participant`, so once the consent pre-stamp was removed (same day), admin-generated `/s/{token}` links died at ConsentGate's `studies` select (PostgREST 406 → "Study not found"). Verified live: the exact failing link renders the consent form post-apply. Mirrors the `20260715_consent_debrief_forms_participant_read.sql` pattern; `study_enrollments` policies don't reference `studies`, so no policy recursion. |
| `20260718_participant_step_timings.sql` | live (applied via MCP `apply_migration`, `participant_step_timings`): `participant_step_timings` table (RLS enabled, 2 policies — `own rows` participant_id=auth.uid() FOR ALL / `lab read all step timings` my_role()='lab' OR is_super_admin()) with a STORED generated `duration_ms` column — verified 13 cols, `duration_ms` is_generated=ALWAYS, RLS on, 2 policies same day; schema smoke-tested with a real insert (42.5s gap → duration_ms 42500, omitting the generated col) then deleted. Generalizes per-video dwell to per-step time-on-screen: `SessionEntry` stamps entry on step mount and inserts one row per step exit (fire-and-forget). SessionEntry-only (admin in-person `StudySessionRunner` deliberately not instrumented — operator-paced, lab client). |
| `20260718_video_advanced_at.sql` | live (applied via MCP `apply_migration`, `video_advanced_at`): `participant_video_sessions.advanced_at timestamptz` (nullable) + `mark_video_advanced(p_session_id uuid)` SECURITY DEFINER RPC (granted anon/authenticated/service_role, mirroring `complete_video_session`) — verified column + function exist same day. Stamps when the participant clicks Continue off the video screen so post-video dwell (`advanced_at - completed_at`) and total time on screen (`advanced_at - started_at`) are measurable; needed because step advancement in `SessionEntry` is client-only (no next-step timestamp) and the video is often a template's final step. Participants have no UPDATE policy on the table (INSERT+SELECT only), so — like completion — the write goes through the RPC, not a direct update RLS would silently block. Idempotent (`WHERE advanced_at IS NULL`). `VideoStepWrapper` captures the sessionId from `StudyVideoPlayer.onComplete` and fires the RPC fire-and-forget on Continue. |
| `20260718_breath_guardian_sessions.sql` | live (applied via MCP `apply_migration`, `breath_guardian_sessions`): `breath_guardian_sessions` table (RLS enabled, 2 policies — `own rows` user_id=auth.uid() FOR ALL / `breath_guardian_sessions: lab write` my_role()='lab', mirroring `drift_performance`/`pond_watch_results`). Flat summary cols (`skin`/`input_mode`/`final_score`/`health`/`self_phase_cycle_sd`) + full research payload in `dataset jsonb` ({session,trials,events,trace}); `session_id`→`game_sessions` ON DELETE CASCADE, `user_id`→`auth.users` ON DELETE CASCADE; two btree indexes (user_id, session_id). Verified 2 policies present + RLS audit (RLS-on/zero-policy check) returns empty for the table same day. Backs the Breath Guardian game (`src/games/BreathGuardian/`, route `/games/breath-guardian`): `saveBreathGuardianSession` inserts one `game_sessions` parent (game_name='breath_guardian') + one detail row on the summary screen, fire-and-forget. |
| `20260715_completion_next_contact.sql` | live (applied via MCP `apply_migration`, `completion_next_contact`): `complete_session_by_token` changed from `RETURNS void` to `RETURNS jsonb` (`DROP` + `CREATE` — a return-type change can't go through `CREATE OR REPLACE`; grants re-issued). Same mutations as before, now also returns `next_contact` (the participant's earliest still-upcoming `participant_schedule` row's `scheduled_date`+`send_time`) and `has_more` (whether `design_graph` continues past the completed session's node — session's own outgoing edge, else owning block's last-child/outgoing-edge check, else owning counterbalance's outgoing edge; `null` for no-graph legacy studies). Powers SessionEntry's "your next session is tomorrow / in N days / later today" completion message. Verified: bogus token returns `{next_contact:null,has_more:null}` with no mutation; `has_more` logic replicated read-only over all 52 session nodes of the Liliana dry-run graph — every node `true` except `s_final` (`false`), exactly as designed. |
| `20260720_zerin_screener_carry_forward.sql` | live (applied via MCP `apply_migration`, `20260720_zerin_screener_carry_forward`): sets `phase2.carry_forward: true` on the Zerin study's `studies.screener` jsonb and the `langerian-mindfulness-v1` `screeners.definition` (data-only, no schema change). Stops the double PHQ-8: the screener's passing PHQ-8 answers are flushed as the baseline row and the "Zerin Baseline" session's PHQ-8 node auto-skips instead of re-administering (`ScreenerPage` marks the draft → `SessionEntry.flushScreenerDraft` drops a `screener_carried_*` sessionStorage marker → `QuestionnaireStepWrapper` reads it and advances without asking). Opt-in flag, so Liliana's `emotion-regulation-v1` screener is untouched; the Zerin Post-Study PHQ-8 (separate session, fresh tab) still administers normally. Verified post-apply: both `carry_forward` flags read `true`. |
| `20260720_completion_next_contact_estimate.sql` | live (applied via MCP `apply_migration`, `20260720_completion_next_contact_estimate`): `complete_session_by_token` (CREATE OR REPLACE, unchanged jsonb signature) now fills `next_contact` from the study DESIGN when no `participant_schedule` row is materialized yet but `has_more` is true. Fixes the vague "you'll receive an email when your next session is ready" seen at a fork gate — e.g. Zerin's post-baseline randomization, whose daily rows materialize ~15 min later on the next cron tick (confirmed: baseline completed 11:45:43, dailies created 12:00:04). Estimate = earliest `study_sessions` row by `ROW(day_number, send_time)` strictly after the just-completed session, anchored to the participant's start date (`scheduled_date - (day_number-1)`); all parallel arms share the same day/time cadence so it's arm-independent. Carries `estimated: true` so the client says "watch for" the link. Verified: for `s_baseline` (day 1, 09:00, 2026-07-20) the estimate resolves to 2026-07-21 09:00, byte-identical to the eventually-materialized daily row. Backs the hardened global completion policy in `SessionEntry.completionMessage` + the de-hardcoded `DailyFarewellStep`. |
| `20260721_pond_watch_pauses.sql` | live (applied via MCP `apply_migration`, `pond_watch_pauses`): `pond_watch_results.pauses integer NOT NULL DEFAULT 0` — per-session count of off-screen pauses (tab hidden / window blurred), the data-quality flag backing Pond Watch's off-screen pause feature; client insert in `PondWatch.jsx` writes it. Verified post-apply: column exists with default 0. |
| `20260722_delve.sql` | live (applied via MCP `apply_migration`, `delve_game_backgrounds_and_metrics`): `delve_backgrounds` table (RLS enabled, 2 policies — `authenticated read backgrounds` SELECT USING true / `lab write backgrounds` my_role()='lab', VAS-scales style since it's a shared content table, not per-user data) + `performance` gains `delve_duration_ms`/`delve_avg_dwell_ms`/`delve_background_id` (FK → delve_backgrounds ON DELETE SET NULL; `delve_` prefix per the game-slug column convention set in the Delve handoff spec) + seed row `delve-backgrounds/prototype-01/prototype-01.jpg`. Verified post-apply: seed row present, security advisors show no delve findings. Image upload to Storage is a separate manual step; the game falls back to a bundled asset until then. |
| `20260723_rrq_rumination_rename_and_midpoint_labels.sql` | live (applied via MCP `apply_migration`, `rrq_rumination_rename_and_midpoint_labels`): data-only fix to two `questionnaires` rows. The scale mislabeled `slug 'rrs'` / "Ruminative Response Scale (RRS)" is actually the **RRQ Rumination subscale** (Trapnell & Campbell 1999 — 12 items, 5-point agree/disagree; verbatim items like "I always seem to be 're-hashing'…"), not the Nolen-Hoeksema RRS (22/10 items, 4-point frequency). Renamed `slug 'rrs' → 'rrq-rumination'` + name → "Rumination-Reflection Questionnaire – Rumination Subscale (RRQ)" to match its existing sibling `rrq-reflection`; migrated the 6 test-only `questionnaire_responses` rows (4 users, no real participants) to the new slug. Also labeled the previously-bare midpoint (`scale_labels[2]`, value 3) as "Neither Agree nor Disagree" on **both** RRQ subscales — they were fully anchored except point 3. Endpoint-anchored scales (brief-maia-2, lms-14, mpod-t, scs-26, sscs-s) deliberately left untouched. Session step resolves the slug via `questionnaires.id` in `get_session_by_token`, so the Sandy Study 3 node needed no change. Verified post-apply: both scales show the 5 labels; 0 leftover `rrs` rows in either table; 6 responses now under `rrq-rumination`. |
| `20260723_export_lab_read_policies.sql` | live (applied manually via Supabase SQL editor 2026-07-23, confirmed by Norm — manual run, no `schema_migrations` history record). Adds a `"<table>: export lab read"` SELECT policy (`my_role()='lab'`) to every table the study Export tab reads (~37, listed in the file), so a lab member's study-level export isn't silently blocked by `own rows` policies. Idempotent DO-block loop (DROP IF EXISTS + CREATE, per-table `undefined_table` guard) — safe to re-run; only touches policies it names. Supports §28a / `src/lib/studyExport.js`. |
| `20260725_tune.sql` | live (applied via MCP `apply_migration`, `20260725_tune` 2026-07-25): `performance` gains `tune_duration_ms`/`tune_avg_dwell_ms`/`tune_scenes_visited` (additive, nullable, `IF NOT EXISTS` — Delve-style summary metrics on the shared table, `tune_` prefix per the game-slug column convention). Verified post-apply: all three columns present (`information_schema.columns`). No new table/RLS — sessions use existing `game_sessions` (game_name='tune'), which is already governed. Backs the Tune game (`src/games/Tune/`, route `/games/tune`); audio clips live in Storage `public-assets/tune-audio/` (uploaded via `scripts/upload-tune-audio.mjs`). |
| `20260725_expired_link_next_session.sql` | live (applied via MCP `apply_migration`, `expired_link_next_session` 2026-07-25). **Replaces `get_session_by_token`** so the expiry branch returns `{error:'expired', next_session:{scheduled_date,send_time}}` instead of a bare error — `next_session` is the earliest still-`pending` row for that participant+study in the future in `America/Toronto`, suppressed for withdrawn/completed enrollments, else null. Pre-apply the live `prosrc` was verified byte-identical to the `20260716_participant_contact_email.sql` body (len 4263, md5 `487bf3c31ea7e345645b7b4b8f04168f`), so the expiry branch is the only delta. Post-apply confirmed: a real expired token returns the new payload with `next_session`, a bogus token still returns `not_found`, and `/s/{token}` renders both the same-day and next-day copy. Supports the expired-link soft landing in `src/pages/SessionEntry.jsx` (v9). |
| `20260725_completed_link_distinct_from_expired.sql` | live (applied via MCP `apply_migration`, `completed_link_distinct_from_expired` 2026-07-25). Fixes a bug in the migration above, found same day: `get_session_by_token` checked expiry **before** completion, so a participant re-opening a link for a session they had finished got the soft-landing "missing one doesn't affect your standing" copy — 96 links were `used` + past-expiry with a completed schedule row at the time. Completion is now checked first and reads from the **schedule** row (`status='completed'` / `completed_at`), returning `{error:'completed', next_session:…}`. Also scopes the expiry branch's `UPDATE … SET status='expired'` to `status='active'` — it was unconditional and had already overwritten `used` on 2 links (repaired by an idempotent UPDATE in this migration; verified 0 remaining). Adds `next_pending_session(uuid,uuid)` so the two terminal branches share one lookup. Post-apply verified by running the live RPC across every `link_status × schedule_status` combination past expiry: used/completed→`completed`, expired/missed→`expired`, expired/blocked→`expired`, revoked/*→`revoked`. Pairs with `SessionEntry.jsx` v10. |
| `20260725_next_contact_must_be_future.sql` | live (applied via MCP `apply_migration`, `next_contact_must_be_future` 2026-07-25). `complete_session_by_token`'s next-contact query selected the earliest row with status IN (`pending`,`link_sent`,`unlocked`) with **no filter on that slot being in the future**, so a missed same-day slot — which stays `link_sent` until the next day, since `check_schedule` step 0b only promotes rows with `scheduled_date < today` — sorted ahead of every genuinely upcoming row and was reported as "next". Norm hit this: after the 20:00 check-in the screen said "later today at 2:00 PM". Adds the same lab-time future predicate `next_pending_session` uses. Verified read-only (the function mutates, so it wasn't invoked on live rows): running the extracted query old-vs-new for the two participants who completed a 20:00 session, the affected one went `2026-07-25 14:00` → `2026-07-26 09:00` and the unaffected one stayed `2026-07-26 09:00`. |
| `20260729_early_gate_advance_next_contact.sql` | live (applied by Norm 2026-07-29, confirmed in-session; authored without live DB access, so no `schema_migrations` record is assumed — check history if it matters). `complete_session_by_token` (CREATE OR REPLACE, unchanged jsonb signature) — the design-based fallback estimate must follow the early-gate pull-forward shipped in `materializeSchedule` the same day: an assessment gating a `randomize` fork now starts the next phase the day after it is completed rather than on its nominal `day_offset`, so an early finisher was told "watch for your link on <day 17>" minutes before the cron pass scheduled day 15. Adds a bounded edge-walk from the completed session's node (only `adherence_check` nodes are transparent, ≤8 hops) to detect a fork gate, and `LEAST`s the estimate against tomorrow **only** in that case — an `adherence_check` gate (e.g. Liliana's `ac_p2` before the final window) is not pulled forward by the materializer and already estimates correctly off the completed row's own shifted `scheduled_date`. `LEAST`-only, so no non-fork study's reported date can move. Verify post-apply against a Liliana participant who completes the midpoint before day 16: the returned `next_contact.scheduled_date` should equal the `participant_schedule` row the next cron tick creates for Phase 2 day 1. |
| `20260729_ripple_cron_credential.sql` | live (applied 2026-07-29 via MCP `execute_sql`, then confirmed by re-reading `cron.job` — a `cron.alter_job` call, so no `schema_migrations` record and none expected). Not schema: repoints the `ripple-reminders-hourly` job's `Authorization` header at the new-style `sb_secret_…` key it had never carried. `ripple_reminder` compares that header to `SUPABASE_SERVICE_ROLE_KEY` byte-for-byte, so the legacy `service_role` JWT the job was created with (2026-07-14) 401-ed every hourly tick for 15 days — **zero Ripple reminders had ever been delivered**, with `cron.job_run_details` reading `succeeded` throughout because that column records the `net.http_post` enqueue, not the response. Evidence of the fix: `cron.job` now matches `sb_secret_` and not `Bearer eyJ` for both active jobs, and the 23:00 UTC tick returned 200 `{"sent":0,"skipped":1,"window":"evening"}` (a correct skip — that user is weekly, 5 days out). The file copies the key from `check-schedule-every-15min` instead of embedding it, so it is idempotent and holds no secret. |
| `20260729_late_gate_estimate_symmetric.sql` | live (applied 2026-07-29 by Claude via MCP `apply_migration`, name `late_gate_estimate_symmetric`, so there **is** a `schema_migrations` record). `complete_session_by_token` again (CREATE OR REPLACE, unchanged signature): the fork-gate estimate now *is* tomorrow by assignment instead of `LEAST(est, tomorrow)`, which could only pull in and therefore left a gate completed **late** naming a date already in the past — 6 days back, measured, on a day-14 gate completed day 23. Reachable on Zerin (baseline holds a 72 h link; arm timepoints sit at day 2). **Applied as a self-patch, not a retyped body**: `pg_get_functiondef` → two asserted `replace`/`regexp_replace` calls → `EXECUTE`, so all 200 other lines are provably byte-identical to what was already running, and the migration aborts if the live definition isn't the early-gate one it expects. Evidence: live `prosrc` is 7228 chars, `md5 = 31296be22619c0c37cb7280f502dc1ae`, **identical to this file's body** (`still_has_least = false`, `has_new_assignment = true`). Behaviour verified against the Liliana DRY RUN day-14 gate link inside a transaction ended by `RAISE` (nothing persisted): on-time → `2026-07-30 06:00 estimated:true`, same gate back-dated to day 14 = Jul 20 (late) → also `2026-07-30 06:00`, where the old expression yields `2026-07-23`. |
| `20260729_null_guard_security_fixes.sql` | live (applied 2026-07-29 by Claude via MCP `apply_migration` as **four** migrations: `is_super_admin_null_safe`, `duplicate_study_null_safe_guard`, `liliana_credit_report_null_safe_guard`, `checkin_report_guards_null_safe` — all four have `schema_migrations` records; this one file records all of them). **SECURITY.** Six `SECURITY DEFINER` functions were callable by anonymous requests: `is_super_admin()` returned NULL (not false) when `auth.uid()` matched no `profiles` row, because the COALESCE sat *inside* the subquery, and `NOT NULL` is NULL, so every `IF NOT is_super_admin()` guard fell through. Proven live with only the public anon key: `admin_list_users` → 200 / 184 rows / every email; `get_liliana_credit_report` → participant rows. Same shape in `admin_set_user_role`, `admin_delete_user`, `duplicate_study`, `get_class_participation`, `get_checkin_quiz_results`, `get_checkin_mood_results`. Verify: `SELECT is_super_admin(), is_super_admin() IS NULL;` → `false, false`, and an anon `curl` of `admin_list_users` → `forbidden: super admin only`. RLS policies were never affected (NULL fails closed in a policy). radlab-academic swept and clean. |
| `20260729_admin_service_diagnostics.sql` | live (applied 2026-07-29 by Claude via MCP `apply_migration`, then two follow-ups: `diagnostics_overdue_canary_excludes_withdrawn` and `diagnostics_comment_sync`; all have `schema_migrations` records). Adds `admin_service_diagnostics()` (super-admin gated, read-only) behind `/admin/diagnostics`, plus helpers `diag_signatures()` / `diag_response_target(text)`. Shape is dictated by what pg_net retains: no url on `net._http_response`, `net.http_request_queue` emptied on completion, ~31 rows of history — so responses are attributed by body signature and the window is reported as bounded. `overdue_pending` initially read 48 on a healthy system (withdrawn-enrollment residue, which nothing ever sweeps) and now excludes withdrawn, reporting them as `withdrawn_residue`. Evidence: live `prosrc` 8178 chars, `md5 = 9fca6dd0a859505d050606f9d95a57ae`, identical to this file's body; gate verified refused with no JWT and the payload verified under the super admin's claims. |
| `20260730_delete_own_account.sql` | live (applied 2026-07-30 by Claude via MCP `apply_migration`, name `delete_own_account`, so there **is** a `schema_migrations` record). Adds `delete_own_account()` for the new Settings page — self-serve deletion keyed on `auth.uid()` with **no target argument**, so it cannot be pointed at another account. **Public tier only**: refuses super admins, refuses any `role <> 'public'` (a participant's rows are study data and withdrawal is RA-mediated; a lab member authors content whose FKs are `NO ACTION` against `profiles`), and refuses anyone holding `study_enrollments` / `participant_schedule` / `participant_links` / `participant_assignments` rows so a future enrolled public account gets an explanation instead of a raw FK error. Delete list derived by enumerating every `public` table with a `user_id`/`profile_id` column against its FK delete action; it also covers three tables with **no** FK at all (`pond_watch_results`, `zerin_daily_checkins`) that would otherwise be left holding a dangling `user_id`. Grants: `authenticated` only, `anon` revoked — verified via `has_function_privilege`. **Note a pre-existing gap this surfaced**: `admin_delete_user` has not been updated since 2026-07-12 and is now missing deletes for `equity_census_responses`, `breath_guardian_sessions`, `pond_watch_results`, `zerin_daily_checkins`, `ripple_unsubscribe_tokens`, plus every participant/lab-authored table (`study_enrollments`, `participant_*`, `liliana_participants`, `message_log`, `classes.created_by`, `displays.created_by`, `slider_scales`, `vas_scales`, `vas_packages`) — so admin deletion of a participant or lab member will raise an FK error. Not fixed here; deleting a lab member's authored content is a separate decision. |
| `20260730_admin_delete_user_refresh.sql` | live (applied 2026-07-30 by Claude via MCP `apply_migration`, name `admin_delete_user_refresh`, so there **is** a `schema_migrations` record). Fixes the gap recorded in the row above: the delete list had not moved since 2026-07-12 and every table added since — plus every participant and lab-authored table that was never in it — holds a `NO ACTION` FK to `profiles`, so `DELETE FROM profiles` raised a constraint violation and aborted the RPC. Admin deletion only ever worked on accounts that had done almost nothing. Adds the 9 missing personal/game tables and the 9 missing participant tables. **Two policy calls**: (1) *participant data is deleted* — the 2026-07-12 header wanted to "preserve data even if a profile is removed", but a `NO ACTION` FK cannot preserve anything, it only makes the delete fail; if a participant's data must outlive them, withdraw the enrollment instead of deleting the account. (2) *lab-authored content is preserved, authorship detached* — `classes`/`displays`/`slider_scales`/`study_consent_forms`/`study_enrollments.enrolled_by` get `created_by = NULL`, matching the `SET NULL` FKs `questionnaires`/`studies`/`study_protocols`/`video_library` already have; `vas_scales.created_by` and `vas_packages.created_by` are `NOT NULL` so those rows are **reassigned to the acting super admin** and the count is returned as `reassigned_vas_rows`. **Ordering fix worth keeping**: `equity_census_responses` holds `NO ACTION` FKs to *both* `participant_schedule` and `study_enrollments`, so it must be deleted before the study-participation block — the first draft had it after and would have aborted. Verified by two dry runs on live data, each a `DO` block ending in `RAISE` so the whole thing rolls back: the participant with the most `participant_schedule` rows deleted cleanly to `profiles`+`auth.users`, and a lab member with authored content deleted cleanly with `vas_reassigned=1`. Counts confirmed unchanged either side (185 profiles / 185 auth.users) and the lab member still owns their VAS scale. `is_super_admin()` null-safety from 20260729 untouched; guards (super admin only, never self, never another super admin) unchanged. |
| `20260730_drop_profiles_reminder_frequency.sql` | live (applied 2026-07-30 by Claude via MCP `apply_migration`, name `drop_profiles_reminder_frequency`, so there **is** a `schema_migrations` record). Drops `profiles.reminder_frequency`, the column behind the Dashboard's `// Reminders` control — a control **nothing ever read**. The `// TODO: reminder emails via Edge Function + Resend` beside it was never built; the engine that does exist (`ripple_reminder`) reads `ripples.reminder_enabled`/`reminder_time`. The UI was removed with the §12b account rework, leaving the column referenced by nothing. Verified before dropping: no function, view, materialized view or index references it, and the repo has only prose mentions. Data at drop: 182 of 184 profiles held the `'none'` default; exactly two had set anything else (one `weekly`, one `biweekly`), and neither ever received an email because no sender existed. Those two values were deliberately **not** migrated into `ripples.reminder_enabled` — enabling somebody's email from a preference they gave a control that visibly did nothing is precisely the opt-in `ripple_spec` §5 guards against; the `weekly` user already has real reminders enabled, and the `biweekly` user has no `ripples` row at all. |
| `20260807_withdrawal_note.sql` | live (applied 2026-08-07 by Claude via MCP `apply_migration`, name `withdrawal_note`, so there **is** a `schema_migrations` record). Adds `study_enrollments.withdrawal_note text` — the optional free-text reason a participant gives on the `/withdraw/{token}` confirmation page, written by `handle_withdraw` (service role, trimmed + capped at 500 chars, empty → null). Deliberately separate from `withdrawal_reason`, which stays the system's classification string. No RLS change (service-role writes; lab reads via existing policies). Displayed in `EnrollmentPanel` under the withdrawn status badge. Verified post-apply via `information_schema.columns`. |
| `20260818_questionnaire_submit_guard.sql` | live (applied 2026-08-18 by Claude via MCP `apply_migration`, name `questionnaire_submit_guard`, so there **is** a `schema_migrations` record). BEFORE INSERT trigger on `questionnaire_responses`: an insert repeating `(user_id, questionnaire_slug)` within **10 seconds** does not create a row — the existing row is UPDATED with the newer responses and timestamp and the insert is skipped. **Why:** participant 909092's BFI-2-S was submitted twice **643 ms apart**, and their Student Stress Scale twice as well. The cost was not an extra row: the export names questionnaire columns by *which administration* a response is, so one double-fire made that participant look like they had three baseline administrations and shifted the timepoint label of every later response they gave. **Why the database and not only the client:** `QuestionnaireRenderer` already refuses to fire `onComplete` twice, but only *per mount*, and a React `saving` flag resets on mount too — so every client-side guard is defeated by a remount, and a 643 ms gap is too slow for same-tick batching and too fast for a human, which points at re-entry rather than a double-click. **Keeps the LATER copy** (matching the export's own de-duplication): if the participant corrected something and resubmitted, the newer answers are the ones they meant. **10 s is deliberately far tighter than the export's 120 s** analysis-time window — this one silently discards a row, so it must only fire on submissions no human could have made deliberately. Existing duplicates are NOT touched; deleting collected research rows is not a migration's decision, and the export collapses them at analysis time anyway. Verified post-apply on a throwaway row: first insert → 1 row; double-fire at +643 ms → still 1 row, holding the corrected responses; genuine re-administration at +30 s → 2 rows. |
| `20260818_liliana_demographics.sql` | live (applied 2026-08-18 by Claude via MCP `apply_migration`, name `liliana_demographics`, so there **is** a `schema_migrations` record). Creates `liliana_demographics` (jsonb `responses` blob, shape mirroring `equity_census_responses`) + 3 RLS policies + the Session Builder activity row `form/liliana_demographics`. **Why a third demographics table:** Liliana's approved design (`demographics-preview.html`, 18 June 2026) is 7 sections / 23 questions, and neither existing instrument matched it. `demographics` collects four items — and Study 3's baseline was authored against *that* one, which is why the live-test export carried only four demographic columns and most of the designed battery was never collected. `equity_census_responses` covers identity thoroughly but the U of T census has no Academic Life or Work & Finances section (student status, campus, faculty, living arrangement, household income, country of birth, primary language, marital status, employment). Widening the census was rejected — it is a faithful reproduction of a published instrument — so the new component **imports the census's option sets and controls** for the identity half, keeping the shared wording from drifting, and defines only the missing sections. RLS mirrors `equity_census_responses` exactly (own-rows FOR ALL + lab read + lab insert); per CLAUDE.md a table with RLS on and no policy silently blocks every write with no client error. Verified post-apply: `relrowsecurity` true with 3 policies, activity row present at 8 estimated minutes. Also wired into the export registry (`Forms` category) with its jsonb flattened into the master under the `ldem_` prefix. |
| `20260817_grounding_requires_full_count.sql` | live (applied 2026-08-17 by Claude via MCP `apply_migration`, name `grounding_requires_full_count`, so there **is** a `schema_migrations` record). Sets `min_required = count` on the five `multi_response` steps of `non-reactivity-phase2-day2`, the 5-4-3-2-1 grounding exercise. The companion to `20260817_strip_intervention_note_fields.sql`: those steps each named an exact number in the prompt ("write down 3 things I can hear") and rendered that many boxes, but set no `min_required`, and the renderer gates on `min_required ?? 1` — so one filled box advanced the screen. The requirement *had* been written down, as a `_note`, which nothing reads. Stripping the notes removed the misleading prose; this puts the one real requirement among them into a key the code honours. Norm, 2026-08-17: "if it asks for 3, let's get 3." **Scope is this one module deliberately** — the corpus's only other `multi_response` steps (`reappraisal-phase2-day5`/`-day6`) read "List **all** of your automatic thoughts" with `count: 3` and an explicit `min_required: 1`, where 3 is a ceiling rather than a quota; forcing three would make participants invent thoughts they do not hold, corrupting the measure rather than tightening it. `count` means "how many boxes to draw", and whether it is also a floor depends on whether the prompt names a number — a judgement no migration should infer, hence the explicit module id over a corpus-wide `min_required = count`. Only fills an **absent** minimum, so a deliberate explicit value can never be overwritten by this sweep; raises rather than proceeding if a `multi_response` step has no `count`. Verified post-apply: all five now `count = min_required` (3/2/5/1/4), both reappraisal steps still `count 3, min_required 1`. |
| `20260817_strip_intervention_note_fields.sql` | live (applied 2026-08-17 by Claude via MCP `apply_migration`, name `strip_intervention_note_fields`, so there **is** a `schema_migrations` record). Data-only: removes every `_note` key from `intervention_modules` step definitions — 19 fields across 6 of the 48 modules. **Why:** `_note` reads like specification and is invisible to `InterventionPage`, which has never read the key. That gap caused two live defects. The first was Graduation Day's four intention follow-ups, whose "CONDITIONAL — only shown if Likert ≥ 2" was honoured by nothing (fixed by `20260812_graduation_day_practice_gate.sql`, which added a real `show_if`). The second was found auditing the rest: **`non-reactivity-phase2-day2`'s five `multi_response` steps — the 5-4-3-2-1 grounding exercise — each say "all required before Next unlocks" while none sets `min_required`**, and the renderer gates on `min_required ?? 1`, so one word per screen advanced. Norm's call was to strip the notes and **not** implement the deviations: the grounding steps stay at the default minimum of one, and `reappraisal-phase2-day7`'s first slider stays required (its "Next always enabled" note predates `NoDefaultSlider` and is deliberately false now). Two self-compassion-day1 claims were checked before deletion and were accurate — `has_text_field` is genuinely set on the "Other" option, and the flagged `lliliana/` typo really was corrected (0 of 38 `video_library` rows carry it), answering a "please verify" that had sat in production data since June. **Renderer-safe by construction**: only `_note` is deleted, and `- '_note'` on a step without one is a no-op, so every step is rebuilt identically and order is preserved. Verified post-apply: 0 `_note` remaining across 193 steps / 48 modules, with `show_if` (15), `min_required` (6) and `point_labels` (3) all untouched, and the Day 12 / Day 2 / Day 7 step shapes unchanged. The deleted text is preserved verbatim in the migration file's header. |
| `20260813_graduation_day_slider_anchors.sql` | live (**applied 2026-08-16 by Norm by hand in the Supabase SQL editor**, not via MCP — the MCP connection was down when it was written, so there is **no** `schema_migrations` record for it; confirm by content, not by the migrations list). Verified post-apply by reading the modules back: all three Graduation Day modules carry the six-element `point_labels` array plus matching `min_label`/`max_label` of `Rarely`/`Almost always`, and the gate from the previous migration is undisturbed (13 steps, 1 gate, 5 `show_if` on each). Data-only: rewords the Graduation Day likelihood slider's span to `Rarely · Occasionally · Sometimes · Often · Very often · Almost always`, setting `point_labels` plus matching `min_label`/`max_label`. **Why:** `20260812_graduation_day_practice_gate` put this item behind an explicit yes/no, which changed *who sees it* — only participants who already answered Yes reach it, so "Never" at the bottom is no longer a reachable answer and contradicts the answer they just gave. Vague quantifiers rather than concrete rates because the item asks how *likely*, not how often; converting it to a frequency measure is a separate call for Liliana. Locates the slider **by type rather than by index** (the gate migration already shifted it once, and a hardcoded position would silently rewrite whatever lands there next), refuses to run if a module has zero or multiple sliders, and asserts the scale is 1..6 before applying labels that assume six points. **Safe to apply after the code is live and safe to leave unapplied**: with no `point_labels` key, `SliderBlock` falls back to `min_label`/`max_label` and renders exactly as it did before, so the two can be deployed in either order. Verify post-apply: `select module_id, definition->'steps'->(…slider idx…)->'point_labels' from intervention_modules where phase='phase2' and lesson=12` — expect the six-element array on all three. |
| `20260812_graduation_day_practice_gate.sql` | live (applied 2026-08-12 by Claude via MCP `apply_migration`, name `graduation_day_practice_gate`, so there **is** a `schema_migrations` record). Data-only: rewrites the `steps` array of the three Phase 2 Day 12 ("Graduation Day!") `intervention_modules`. Inserts an explicit yes/no `training_response` (`key='will_practice'`, "Do you think you will use any of these practices in your everyday life?") after the five open-ended reflections, and puts the likelihood slider **plus** the four intention follow-ups (when / where / barrier / overcoming) behind `show_if {key: will_practice, equals: Yes}`. "Do you have any additional comments" and the closing stay unconditional, so a No answer lands there directly. **Why:** all four follow-ups already carried `"_note": "CONDITIONAL — only shown if Likert response is 2 or higher (not Never)"`, but `_note` is inert prose — nothing read it, and `InterventionPage` navigation was strictly linear — so every participant was walked through "When will you practice? How often?" *including* anyone who had just answered "Never". The intent shipped in the data; only the mechanism was missing. Norm's call (2026-08-12) to make the gate an explicit question rather than a threshold on the slider: it reads better than an invisible cutoff, and it retires the open "should the scale start at 0 = Never?" question for this item, since Nevers no longer reach the slider at all. Keeping the slider behind the gate (rather than dropping it) preserves the graded intention measure for everyone who intends to practise. The rewrite is positional, so it **asserts the shape first** (12 steps, index 5 = slider, index 11 = closing) and raises rather than scrambling a module that has drifted; it is idempotent via a `will_practice` guard, and drops the now-stale `_note` keys so prose and logic cannot disagree again. Requires the `show_if` support added to `src/components/study/InterventionPage.jsx` in the same commit — **do not apply this migration to a deployment without that code**, or the gate renders as an ordinary question and the follow-ups all show regardless. Verified post-apply: all three modules 13 steps, 1 gate, 5 `show_if`, 0 `_note`. Existing Day 12 responses were two test rows only, so the `response_index` shift costs nothing. |
| `20260811_adherence_on_fail.sql` | live (applied 2026-08-11 by Claude via MCP `apply_migration`, name `adherence_on_fail`, so there **is** a `schema_migrations` record). Two parts. (1) Sets `on_fail` on every `adherence_check` node in every `design_graph` that has one — `'continue'` on the phase-2 checks, explicit `'withdraw'` on phase 1 — which today is exactly `ac_p1`/`ac_p2` on the two Liliana studies (DRY RUN + Live Test). Scoped by node shape rather than hardcoded study ids; `WITH ORDINALITY … ORDER BY` preserves node order, which `entryNode()` and the builder layout both read. (2) Creates the `liliana_phase_adherence` view (`security_invoker`) — one row per participant per authored check, counting completed `liliana_day_data` rows by module phase, i.e. exactly what `countCompletedPhaseDays()` counts. Verified post-apply: all four nodes carry the intended `on_fail`, and the view returns the expected split (Live Test's 7/12 phase-2 participant is `meets_criterion=false`, `on_fail=continue`). |
| `20260811_sandy3_screener.sql` | live (applied 2026-08-11 by Claude via MCP `apply_migration`, name `sandy3_screener`, so there **is** a `schema_migrations` row). Seeds the `perfectionism-effort-v1` screener and points Sandy Study 3 at it (`screener_id` + denormalised `screener` jsonb). First **phase-1-only** screener in the library — no questionnaire phase — which required `ScreenerPage.jsx` to stop assuming one; before that change, passing the criteria dead-ended on a "Continue to Pre-Screening" button with nothing behind it. Idempotent (`ON CONFLICT (slug) DO UPDATE`), so re-running only refreshes the copy. Verified after apply: 5 items, `has_phase2` false, pass/fail messages and 4 crisis resources present. |
| `20260818_roster.sql` | live (applied 2026-08-18 by Claude via MCP `apply_migration`, name `roster`). WP5. Creates `identity.roster` (PII stays in the identity schema; RLS on, **zero policies** — service-role and definer-function access only) and `identity.roster_match_attempts` (the unmatched-join queue, IP stored as a truncated hash). Six functions: `normalize_uoft_email` (collapses @mail./@alum./@utoronto.ca to one match key), staff-gated `roster_upsert` (idempotent CSV import that never regresses status), `roster_admin`, `roster_set_status` (cannot set `enrolled` — that transition belongs to `enroll_from_roster` alone), `roster_attempts`/`roster_resolve_attempt`, and `enroll_from_roster()` — runs as the signed-in user, matches the auth email's normalized key against the roster, grants the student enrollment; the session only exists because an emailed link was clicked, which is the §2a.4 'mailbox proven' invariant. Verified in a rolled-back transaction: import of 4 rows → 2 inserted, 1 updated (the @utoronto.ca duplicate matched its @mail.utoronto.ca row through the normalizer), 1 skipped (malformed); `roster_admin` returns rows for staff and **0 for a non-staff sub**. Serverless pair `api/roster-invite.js` (staff bearer token, admin.generateLink + Resend send — bypasses the auth mailer's rate limit entirely, §2a.6) and `api/roster-join.js` (public, cooldown + lifetime cap, unmatched → attempts queue). **Not yet testable end-to-end: `RESEND_API_KEY` must be set in Vercel env** (the functions 500 with a named missing list until it is). |
| `20260813_page_reviews.sql` | live (applied 2026-08-13 by Claude via MCP `apply_migration` as **two** server migrations: `page_reviews` and `page_gaps_staff_kind` — the second because the `page_gaps.kind` CHECK predated `'staff'` and the first smoke test tripped it; the repo file carries both). WP6 Phase D second half. Creates `page_reviews` (stamp history — page, version-at-stamp, reviewer, `clear`/`needs_work`, note; staleness computable as stamp.version < current_version), staff-only SELECT policy, writes only through `stamp_page()` (no insert policy — the claim-flow door pattern). `flag_gap()` inserts reader-flagged gaps as `kind='staff'` owning the ask_hash convention (`md5(lower(normalized ask))`) so reworded duplicates dedup; capacity 2 for green, else 1. `whats_new(course_id)` SECURITY DEFINER feeds the class changelog: accepted versions on published pages — accepted *proposals* (`kind='proposed'`, version always NULL, landed at `reviewed_at`) union job-less *snapshots with notes* (edit_page corrections, landed at `created_at`); job-less snapshots without notes are excluded as mechanical echoes of proposal writes, or every ingest update would list twice. `examinable` = a clear stamp postdating the landing (timestamp comparison — proposal rows carry no version number to compare). Returns no names and no stamp notes. Verified in rolled-back transactions: `stamp_page`/`flag_gap` succeed under Norm's JWT (stamp v5 clear on `little-albert-study`; staff gap green/cap 2/hash correct), both refuse an unenrolled sub with `staff only`, `whats_new` returns 0 rows pre-publish (correct — nothing is published) and 0 for a non-member. |
| `20260813_ripple_cadence_expand.sql` | live (applied 2026-08-13 by Norm via the Supabase SQL editor, Supabase MCP was disconnected at the time; confirmed post-apply by Claude via MCP `execute_sql` reading `pg_get_constraintdef` once MCP reconnected). Widens `ripples.prompt_cadence`'s CHECK constraint to add `every_other_day` / `every_other_week` (Account/My Ripple redesign, website.md §12b revised). `ripple_reminder` redeployed the same day to match (version 4 → 5, via MCP `deploy_edge_function`, confirmed live post-deploy by re-fetching with `get_edge_function`). |
| `20260813_sandy3_screener_fail_copy.sql` | live (applied 2026-08-13 by Claude via MCP `apply_migration`, name `sandy3_screener_fail_copy`, so there **is** a `schema_migrations` row). Targeted `jsonb_set` on `phase1.fail_message.body` only, in the library row and the study's denormalised copy. The screen-out now names the **study description** as the reference point for the return request, so it reads as a published rule rather than a verdict on the participant. Verified after apply: body updated, all other fields byte-identical, study copy back in sync, and the rendered screen shows the bold with no literal tags. |

## radlab-academic project migrations

These files target the **separate `radlab-academic` Supabase project** (courses/enrollments/identity
schema for the academic partition), **not** the main radlab project everything above refers to.
Don't look for their effects in the main project's schema.

| File | Applied — evidence |
|---|---|
| `20260724_academic_init.sql` | Applied to `radlab-academic` manually by Norm (2026-07-24, confirmed by Norm — no `schema_migrations` history record). Creates `identity` schema (`identity.people` + signup trigger `on_auth_user_created`), `public.current_person_id()` helper, `courses`, `enrollments`, `invites` (service-role only, deliberately no authenticated policies), `ingest_jobs` (staff-read RLS via enrollments), storage upload policy for bucket `ingest-pdfs`, and PSY240 2026F seed data + staff invites. Supersedes the flat `ingest_staff` design in the partition plan §4. |
| `20260725_academic_wiki_schema.sql` | Applied to `radlab-academic` manually by Norm (2026-07-25, confirmed by Norm via `supabase/checks/wp1_verify.sql` returning OK on all seven checks — no `schema_migrations` history record). WP1 of the Field Guide wiki: `dsm_chapters` (seeded, 20 rows, verified DOI slugs), `wiki_pages` (+ generated `tsvector` + GIN), `wiki_page_versions` (accepted history *and* pending proposals), `wiki_links` (with unresolved red links), `disorders` (shape only — rows are WP3), the `disorder_criteria_links` view, `is_course_member()`/`is_course_staff()` helpers, and five triggers (version snapshot, version bump, link extraction, link resolution both directions). Read policies are roster-gated via `enrollments`; **authenticated write policies are deliberately absent** (service-role writes only, as with `invites`/`ingest_jobs`) — see the RLS NOTE in the file before "fixing" that. Behavior was verified locally first (Postgres 16, stubbed `auth`/`storage`): clean apply on top of the init migration, plus a suite covering versioning, link binding, proposal isolation, criteria-URL assembly, search, and the student/staff/outsider RLS split. **Re-check applied status any time with `supabase/checks/wp1_verify.sql`** in the radlab-academic SQL editor — read-only, safe to re-run. Its check 5 is worth keeping past this migration: it fails if anyone "corrects" `.x14_Gender_Dysophoria` or re-expands a truncated chapter slug, a silent five-404 regression nothing else catches. |
| `20260725_academic_wiki_wp3.sql` | Applied to `radlab-academic` 2026-07-25 by Claude via the Management API query endpoint (no `schema_migrations` history record). WP3 part 1: seeds all **123** taxonomy catalog rows into `disorders` (46 Tier A / 52 Tier B / 16 overviews / 9 foundations, 18 DSM chapters; idempotent `on conflict (course_id, slug) do nothing`), links catalog rows to already-ingested pages by slug, and adds the review path — `review_proposal(uuid, text, text, boolean)` (SECURITY DEFINER, internal staff check, so `wiki_pages` still has **no** authenticated write policies) plus the `review_queue` view. Dry-run first inside `BEGIN … ROLLBACK` with assertions (123 rows, tier split exact, 0 duplicate slugs, both rewrite-flags present), then applied. The RPC was then exercised against live data in a second rolled-back transaction under the instructor's JWT: accept published a page and fired the snapshot + link-extraction triggers (3 links, all resolved), reject left the page body null, queue 26 → 24. Verify with `select count(*) from disorders` (expect 123) and `select count(*) from review_queue`. **Note: both this migration's `review_queue` and WP1's `disorder_criteria_links` shipped without `security_invoker` — fixed by `20260726_academic_wiki_view_security_invoker.sql`, see below.** |
| `20260726_academic_wiki_view_security_invoker.sql` | Applied to `radlab-academic` 2026-07-26 by Claude via the Management API. **Security fix.** In PostgreSQL a view runs with its *owner's* privileges, so RLS on underlying tables is evaluated against `postgres`, not the caller — and Supabase grants SELECT on public views to `authenticated` by default. Both `disorder_criteria_links` (WP1) and `review_queue` (WP3) therefore bypassed the roster gate entirely. Measured as an authenticated user with **no enrollment**: the tables correctly returned 0 rows, but `review_queue` returned all 26 unreviewed proposal bodies and `disorder_criteria_links` the full 123-row catalog. `alter view … set (security_invoker = true)` on both (PG15+; this project is 17.6). Re-verified after: stranger 0/0, instructor still 26/123/22. **Convention: every view over a roster-gated table in this project must set `security_invoker`** — the narrow-privileged-read pattern here is a SECURITY DEFINER *function* with an explicit membership check (`review_proposal`), never a view. |
| `20260726_wiki_links_both_syntaxes.sql` | Applied to `radlab-academic` 2026-07-26 by Claude via the Management API. `sync_wiki_links()` only matched `[[wikilink]]` syntax, but the model does not use it consistently — measured across the 26 queued proposals: of 22 `action=new`, 11 use `[[...]]` and 5 use `[text](slug.md)` **only**; of 4 `action=update`, **zero** use `[[...]]` and all 4 use markdown links only. So 9 of 26 pages contributed nothing to the link graph, silently — which matters because backlinks and the red-link count are what taxonomy §5's Tier B argument is measured against, and an empty graph reads exactly like a well-connected one. Now extracts both, with markdown matching deliberately narrow (relative `.md` targets only; anything with a scheme or a path separator is skipped). Includes a backfill (`update wiki_pages set content = content`) that re-derives links for existing pages. Verified: links went 2 → 11 across the reviewed pages, 0 red. |
| `20260726_wiki_publish_events.sql` | Applied to `radlab-academic` 2026-07-26 by Claude via the Management API. Adds `wiki_page_events` (publish/unpublish audit: event, reason, actor, staff-read RLS), `unpublish_page(uuid, text)` (SECURITY DEFINER, staff check, **reason mandatory**, returns the page to `draft` keeping content and review history; refuses a page that isn't published), and the `wiki_page_shelf` view (accepted pages with status, version, link counts, pending-proposal count, last unpublish reason — `security_invoker` set). Also **hardens `review_proposal`**: accepting an `action=update` proposal verbatim onto a page with no accepted body now raises, because the UI warning was demonstrably clicked past (`empirical-support-for-psychodynamic-therapy` was published as a bare addendum while its own full proposal sat pending). Editing the delta into a standalone page is still allowed — that arrives as `p_content`. And publishing via `review_proposal` now logs a `published` event so publish/unpublish read as one history. Dry-run verified all eight behaviours in a rolled-back transaction: raw delta refused, edited delta allowed, publish logged, blank reason refused, unpublish → draft, unpublishing a draft refused, both events logged with actor and reason, content survived. No bulk publish by design. |
| `20260726_wiki_page_needs.sql` | Applied to `radlab-academic` 2026-07-26 by Claude via the Management API. Disorder pages now declare their own gaps: the ingest prompt requires six fixed H2 sections (Presentation / Diagnosis / Epidemiology / Etiology / Treatment / Contested) plus a frontmatter `needs: [...]` list naming the ones the source couldn't support. Adds `wiki_pages.needs text[]`, `extract_page_needs()` (tolerates quotes and spacing), a BEFORE trigger that populates it on every content write, `needs`/`gap_count` on the rebuilt `wiki_page_shelf` view (dropped and recreated — `create or replace view` can't insert a column mid-list), and the new `wiki_gap_report` view joining the 123-row catalog to page state: *no page yet* / *shell only* / *gaps* / *complete*. Rationale: reviewing the first 26 proposals, both disorder pages came from psychotherapy-efficacy papers and were therefore entirely treatment-shaped, with no diagnosis or etiology and nothing marking either absent — a page that silently omits etiology is indistinguishable from one where etiology is settled (Norm, 2026-07-26). Verified in a rolled-back transaction: parsing of simple/quoted/empty/absent lists, trigger population, shelf exposure, and the report reading 121 *no page yet* + 2 *complete* against the live catalog. |
| `20260726_ingest_source_type.sql` | Applied to `radlab-academic` 2026-07-26 by Claude via the Management API. WP3's last piece: `ingest_jobs.source_type` (`paper` default | `reference`) + `target_slug`, with a CHECK enforcing that a reference run names its target and a paper run does not. Adds the `reference_worklist` view — incomplete catalogue pages ordered Tier A first, then by state and gap count, with the count of reference runs already attempted per page. Not a second pipeline (plan §2.3): same function, bucket, job table and review queue, just a discriminator and a second system prompt. Verified in a rolled-back transaction: both constraint directions refused, valid reference job accepted, 8 existing jobs defaulted to `paper`/null, worklist 121 with Tier A first. |
| `20260726_disorders_page_link_trigger.sql` | Applied to `radlab-academic` 2026-07-26 by Claude via the Management API. `disorders.page_id` was set by a one-off UPDATE in the WP3 seed and never maintained — after two further ingests it read 2 linked / 5 stale. Harmless so far (every view joins catalogue to page on `(course_id, slug)`, not through this column, so `wiki_gap_report` and `reference_worklist` were always correct) but a denormalised pointer that only drifts misleads whoever reads it next. Backfills and adds triggers in both directions — a page appearing or being renamed, and a catalogue row being seeded. Kept rather than dropped because it is a genuine FK a later UI can follow without a slug round-trip. |
| `20260803_academic_ta_invite.sql` | Applied to `radlab-academic` 2026-08-03 by Claude via MCP `apply_migration` (name `academic_ta_invite`, so there **is** a `schema_migrations` record). Data only: one `invites` row, `kavabee@gmail.com` / `ta` / PSY240 2026F, idempotent. No new role and no main-project change — the academic partition has had `enrollments.role in ('student','ta','instructor')` since the init migration, so "add a TA" is a roster insert, not a schema or auth change. Verified: `invites` now reads 2 rows, the new one `consumed_at` null (it stays null until she signs up at `/academic/fieldguide`, when `identity.handle_new_user()` consumes it into an enrollment). |
| `20260726_version_numbering_from_history.sql` | Applied to `radlab-academic` 2026-07-26 by Claude via the Management API. **Bug fix.** Accepting a proposal failed with `duplicate key value violates unique constraint "wiki_page_versions_accepted_idx"`. `wiki_pages_touch()` derived the next version from the cached `wiki_pages.current_version` rather than from the version history the unique index is actually over. The two agree until they don't — clearing a page's body for regeneration while deliberately keeping its accepted snapshots reset `current_version` to 1 beneath snapshots already at v1 (and v1+v2), so the next accept recomputed v1 and collided. Now the version is `max(version)+1` among that page's accepted snapshots, so numbering self-heals after any reset, restore or manual fix; also realigns the cached counter where it had already diverged. Verified by reproducing the failure on the unpatched trigger and then accepting cleanly at v2 and v3 with all earlier snapshots preserved. |
| `20260727_reference_worklist_chapter.sql` | Applied to `radlab-academic` 2026-07-27 by Claude via the Management API. Rebuilds `reference_worklist` with `chapter_title` (joined from `dsm_chapters`, foundations bucketed as *Foundations & course-wide*) and `chapter_sort`, ordered chapter → overview → Tier A → Tier B. The reference-mode target picker was a flat list of 121 catalogue pages; it is now grouped into 19 `<optgroup>`s of 1–11, which also matches how the WP4 sprint runs — one Bridley & Daffin module maps to one DSM chapter. |
| `20260727_source_attribution.sql` | Applied to `radlab-academic` 2026-07-27 by Claude via the Management API. Attribution had been left to the model via a `sources:` frontmatter block; measured live, **all 28 pages with accepted content carried none**, because that instruction existed only in the reference prompt until 2026-07-26. The deeper problem is that it was ever the model's job — for CC BY-NC-SA material attribution is a **licence condition**, so it cannot depend on prompt compliance or on which mode the operator picked. Adds `ingest_jobs.source_citation` (captured at upload, now a required field in the portal), backfills the nine existing jobs — the three research-paper citations taken from the `study` pages the model itself extracted, DOIs included, rather than invented — and adds `wiki_page_provenance`, which derives each page's attribution from the jobs whose proposals were accepted into it. Surfaced on the rebuilt `wiki_page_shelf` with `source_count` and a `has_unverified_source` flag. Result: **39/39 pages attributed, 0 unverified**. |
| `20260730_wiki_body_needs_and_replace.sql` | Applied to `radlab-academic` 2026-07-30 by Claude via MCP `apply_migration` (name `wiki_body_needs_and_replace`). Two fixes to the gap mechanism, both surfaced by the WP2 reader making duplicated disorder skeletons visible — `major-depressive-disorder` carried the six sections **three times**, `persistent-depressive-disorder` twice, one copy per accepted `update`. **(1) `needs` is now derived from the body, not the frontmatter.** `extract_page_needs()` read the frontmatter `needs:` list, taking the first match in the document, while the review UI's merge pre-fill deliberately strips an addendum's frontmatter (correctly — two YAML blocks in one file is invalid), so the gap list froze at the first source's assessment. It was measurably wrong: MDD declared 4 gaps and PDD 1, and **all five were already filled further down the same page**. That is not cosmetic — `reference_worklist` reads `needs` and is what aims WP4's content sprint, so the sprint would have chased closed gaps while missing new ones. The rule now: a section is a gap when no copy of it contains prose (H2 opens a section, H1 is the title, H3 belongs to the section above; a blockquote is the `> **Needs research:**` marker and never counts as prose). Self-correcting, and it also catches the model's own omissions — the dry run found `cyclothymic-disorder` missing `etiology` and `disruptive-mood-dysregulation-disorder` missing `epidemiology`, both with written placeholders never listed in frontmatter. **Verified as a shadow function against all 44 live pages before swapping: 40 unchanged, and the 4 that moved were exactly those four corrections.** Backfill is `update wiki_pages set content = content`, which is safe here because `wiki_pages_snapshot` and `wiki_pages_touch` both short-circuit on unchanged content/title/summary — confirmed after: 0 new version snapshots, 0 rows touched, `updated_at` still 2026-07-27. **(2) A third proposal action, `replace`.** The duplication's root cause was two prompt rules contradicting each other — six H2 sections "ALWAYS" on disorder pages, and an update carrying "only the new information to merge" — so every update emitted a full skeleton that the pre-fill appended. Reference mode now returns a complete page with `action: 'replace'`, which overwrites instead of appending; paper-mode updates stay deltas but no longer carry the skeleton. A distinct action rather than a heuristic on content shape, because the review UI must not pre-merge it. `review_proposal()` needed no change: its delta guard fires only on `action='update'` with no existing body. Verified with a self-aborting `DO` block: `replace` accepted, `bogus` rejected by the CHECK, both rolled back, 0 rows left behind. |
| `20260730_wiki_edit_page.sql` | Applied to `radlab-academic` 2026-07-30 by Claude via MCP `apply_migration` (name `wiki_edit_page`). **The wiki had no edit path at all.** `review_proposal()` needs a *pending* version so it can only accept what an ingest proposed, `unpublish_page()` only hides a page, and `wiki_pages` deliberately has no authenticated write policies — so an accepted page could not be corrected by anyone: not a duplicated skeleton, not a wrong prevalence figure, not a typo. Unnoticed until the WP2 reader made two malformed pages visible. Adds `edit_page(p_page_id, p_content, p_note)` — SECURITY DEFINER with an internal `is_course_staff()` check, matching `review_proposal`/`unpublish_page`, so there is still exactly one client-reachable way to change a body and `wiki_pages` still has no write policies. Plus `wiki_page_versions.note` for the human "why". Three deliberate choices: **history is automatic** (the existing `wiki_pages_snapshot` trigger records the previous body as an accepted version and `wiki_pages_touch` bumps the version, so the function only labels the snapshot the trigger just made); **the note is optional**, unlike unpublish's mandatory reason, because an edit leaves a diff and a forced field mostly collects the word "fix"; and **blanking is refused** — a bodiless page is a *shell* awaiting its first proposal, so emptying a live page is a retirement (`unpublish_page`) rather than a correction. A no-op is refused too, so a double-clicked Save doesn't accumulate identical snapshots. Verified in one rolled-back transaction impersonating the instructor's JWT: blank refused, no-op refused, non-staff refused, and a real edit moved `current_version` 1→2, added an accepted snapshot (1→2 — the old body survives), attached the note to the right version, extracted the new wikilink, and **re-derived the gap list, clearing `etiology` because the edit filled it** — the self-correcting property of the body-derived `needs` demonstrated end to end. Rollback confirmed: version back to 1, 0 notes, 57 snapshots. Staff-only edit affordance on the reader (`WikiPage.jsx`), which re-fetches after a save rather than patching its own copy. |
| `20260730_wiki_needs_partial_sections.sql` | Applied to `radlab-academic` 2026-07-30 by Claude via MCP `apply_migration` (name `wiki_needs_partial_sections`). Refines the same-day body-derived gap rule, which defined a gap as "no copy of this section holds prose" and so silently lost the **partially filled** case: a section with real content that still carries an explicit `> **Needs research:**` marker read as complete. Found while merging `persistent-depressive-disorder` — its Epidemiology, Etiology and Treatment sections each have genuine prose *and* a marker naming what is still missing, so after the merge the page reported `needs = {}` while its own body asked for research in three places, and `reference_worklist` (which aims WP4) would not have targeted any of them. Rule now: **a section is a gap if it declares a marker, or if it has no prose at all** — the marker is the page's own statement about itself and wins, and an empty unmarked section is still a gap, so nothing from the previous rule is lost. This matches what the prompt means by the marker ("for every section the source does not support") — a *sourcing* gap, not a scientific controversy, since controversies belong in Contested. Verified as a shadow function against all 44 live pages first: **exactly one page changes** (`persistent-depressive-disorder`, `{}` → `{epidemiology, etiology, treatment}`), every other page identical under both rules. |

| `20260730_promote_personality_disorders_tier_a.sql` | Applied to `radlab-academic` 2026-07-30 by Claude via MCP `apply_migration` (name `promote_personality_disorders_tier_a`). Norm's call: the seven Tier B personality disorders promoted to Tier A, so all ten PDs get a full page. Resolves a flag the taxonomy seed carried on the `narcissistic-personality-disorder` row from the day it was written — "this whole chapter is the tier split most likely to be wrong: L11 gives all ten PDs full treatment. Promoting the seven Tier B PDs is +1.5 h review and the cheapest place to spend a spare hour." The tier split is a *review-budget* device, and here it disagreed with what the lecture actually does; a student who has just had a full lecture on narcissistic PD and then finds a 150-word stub concludes the wiki is thin. **Tier A 46 → 53, Tier B 52 → 45**, 123 catalogue rows unchanged (this moves pages between tiers, it adds none). Cost ~+1.5 instructor-hours in WP4, so fall scope becomes 78 generated pages at ~17 review hours. Also reorders `reference_worklist`, which sorts Tier A first, so those seven now rank above the remaining stubs for the content sprint — the intent. Each promoted row records the decision in `tier_review_note`, appended to any existing note rather than replacing it. `antisocial`, `borderline` and `schizotypal` were already Tier A and untouched; verified afterwards that all 10 lecture-11 PD rows are Tier A and 0 remain Tier B. |

| `20260731_wiki_rename_archive.sql` | Applied to `radlab-academic` 2026-07-31 by Claude via MCP `apply_migration` (name `wiki_rename_archive`). Adds `rename_page(page_id, new_slug, new_title)`, `archive_page(page_id, reason)` and `restore_page(page_id, reason)` — same audited-SECURITY-DEFINER shape as `review_proposal`/`unpublish_page`/`edit_page`, so `wiki_pages` still has no authenticated write policies. Prompted by the first paper-mode foundations run: Module 01 produced `abnormal-behavior` where the catalogue says `what-is-abnormal`, the content was good, and the page had already been accepted — leaving a choice between a duplicate page and a whole module re-read to regenerate content that already existed. **Rename rather than delete**, because deleting loses version history and provenance and turns every inbound wikilink into a pointer at nothing. **The bit the existing triggers get wrong:** `wiki_pages_bind_links()` only ever *binds* — it sets `target_page_id` on links naming the NEW slug but never unbinds links that named the OLD one, so a rename would leave `wiki_links` claiming a healthy graph while the reader (which resolves through the slug map) showed a dead link; `link_disorder_page()` has the identical gap on `disorders.page_id`. Rather than change two triggers other paths depend on, `rename_page` cleans up after itself and **reports what it orphaned** — a rename that breaks three inbound links says so. `archive_page` reaches a state the schema already had (`archived` was in the status CHECK, and `api/ingest.js` already excludes archived pages from the model's index) but nothing could set; reason mandatory, same reasoning as unpublish. `restore_page` returns to *draft*, never straight to published. `wiki_page_events` gains `archived`/`restored`/`renamed`. Verified in one rolled-back transaction under the instructor's JWT: bad slug, collision and same-slug all refused; a real rename moved `abnormal-behavior` → `what-is-abnormal` with the catalogue row linked and **2 inbound links reported orphaned**; blank reason refused, archive→restore round-tripped, non-staff refused. |
| `20260801_participant_links_ended_reason.sql` | Applied to `radlab` (main project) 2026-08-01 by Claude via MCP `apply_migration` (name `participant_links_ended_reason`). Adds `participant_links.ended_reason` (CHECK `timeout`/`superseded`/`admin_revoked`/`withdrawn`), `ended_at`, `ended_by`, plus a partial index. **Why:** `status` was answering two questions at once — what the participant should see, and why the link ended — and it answered the second not at all, because both terminal values were overloaded. `'revoked'` was written by an admin clicking Revoke, by the one-live-link supersession in `_shared/issueLink.ts`, *and* by adherence withdrawal; `'expired'` by the `check_schedule` timeout sweep and by client-side supersession. So "did someone cancel this, or did it time out?" was unanswerable — of 15 revoked rows, 8 had a completed schedule row and 7 did not, with nothing to separate them. Splitting the cause out lets `status` mean one thing to a participant (`expired` = this window closed, nothing to do; `revoked` = something deliberate happened, talk to us). **Backfill was deliberately partial**: all 214 `expired` rows were set to `timeout` — safe because the only other writer of `'expired'` was `lib/scheduleGenerator.issueLink`, which threw a `ReferenceError` from 2026-06-02 until it was repaired 2026-07-31 and so never wrote a row, and confirmed against the data (all past `expires_at`, none with a completed row). The 15 `revoked` rows were left NULL rather than guessed, so NULL honestly means "ended before this column existed". Shipped with the five call sites updated to populate it, and with `_shared/issueLink.ts` switched from `'revoked'` to `'expired'` + `superseded` and narrowed from participant-scoped to **per-study** — see website.md. |
| `20260801_delve_mosaic_mandala_02_06.sql` | Applied to `radlab` (main project) 2026-08-01 by Claude via MCP `apply_migration` (name `delve_mosaic_mandala_02_06`). Data-only: inserts `delve_backgrounds` rows for Mosaic Mandala 02–06 (`delve-backgrounds/mosaic-mandala-0X/mosaic-mandala-0X.jpg`, sort_order 2–6). Verified post-apply: table holds 7 active rows. Storage objects uploaded separately by Norm; until an object lands the game's `img.onerror` path shows the bundled default, so the rows are safe ahead of upload. |
| `20260801_final_notice_reminder.sql` | Applied to `radlab` (main project) 2026-08-01 by Claude via MCP `apply_migration` (name `final_notice_reminder`). Adds `participant_schedule.final_notice_sent_at` and extends `message_log_kind_check` with `'final_notice'`. Backs the deadline-anchored "last chance" reminder on critical sessions (see website.md §26a). **Why a column rather than reusing `last_sent_at`:** the notice is deliberately exempt from `reminder_max`/`max_attempts`, so without its own once-only marker the 15-minute cron would re-send it every tick for the whole 12 h lead window; `last_sent_at` can't serve because cadence reminders share it. **Why its own `message_log` kind:** the final notice is the only session email that states a consequence ("your participation ends at this point"), so "was this participant actually warned before being withdrawn?" has to be answerable from `message_log` alone — the same reason `adherence_termination` is distinguished from `session_link`. Verified post-apply: column present as `timestamp with time zone` with its comment, CHECK now lists all three kinds. Additive only — no backfill, no existing row touched. |

| `20260731_catalogue_internet_gaming_disorder.sql` | Applied to `radlab-academic` 2026-07-31 by Claude via MCP `apply_migration` (name `catalogue_internet_gaming_disorder`). Adds one `disorders` row. Module 15's reference run produced `internet-gaming-disorder` from the module's closing "Check This Out" box — a good page, but with no catalogue row it sat *outside* the catalogue entirely: reachable by link and by search, invisible in the wiki index's browse-by-DSM-chapter view, and absent from `reference_worklist`, so nothing would ever have aimed a run at its declared etiology and treatment gaps. Placed at **DSM chapter 16, lecture 8, tier B**, beside `gambling-disorder` (ch 16, tier A, L8). The *addictive behaviours* half of that chapter is the right home rather than substance use as such — DSM-5-TR groups non-substance addictions there and ICD-11 files gaming disorder under "disorders due to substance use or addictive behaviours." Tier B because it is a **Section III condition for further study**, not a formal DSM-5-TR diagnosis, while ICD-11 *does* list gaming disorder: the page is short by design and its teaching value is the disagreement, not the criteria (that reasoning is in `tier_review_note`). Idempotent (`NOT EXISTS` guard) and resolves `course_id`/`page_id` by lookup rather than hardcoded UUIDs. **Catalogue 123 → 124 rows** (Tier B 45 → 46) — a number quoted throughout the taxonomy and run-plan docs, so expect it to disagree with them until they are next revised. Fall generated-page scope is unchanged at 78: the page already exists. Verify with `select count(*) from disorders` (expect 124) and `select * from disorders where slug='internet-gaming-disorder'` (expect `page_id` non-null). |
| `20260731_split_panic_agoraphobia.sql` | Applied to `radlab-academic` 2026-07-31 by Claude via MCP `apply_migration` (name `split_panic_agoraphobia`). Norm's call. Module 07's paper run produced `panic-disorder` and `agoraphobia` as **two** pages against a catalogue carrying them as the single slug `panic-disorder-and-agoraphobia` — the first failure of the slug-convergence assumption that justifies paper mode for the disorder chapters. The model has the better case: DSM-5 promoted agoraphobia to an independent diagnosis (a specifier of panic disorder only in DSM-IV), so the combined slug reflected how L3 groups them rather than how the manual lists them. Left alone it was a standing inconsistency — the combined row read "no page yet" in `reference_worklist`, so it would have kept surfacing as unwritten work, while two off-catalogue pages holding exactly its content sat beside it with ten inbound links and no chapter placement in the index's browse view. Repoints the existing row at `panic-disorder` and inserts `agoraphobia`, same chapter 5 / tier A / lecture 3. `page_id` is set explicitly on both because `link_disorder_page()` fires on row *insert*, not on a slug UPDATE. **Catalogue 124 → 125, Tier A 53 → 54**; fall generated-page scope 78 → 79, though both pages already exist so what actually grew is the review burden. Verify: `select slug, page_id is not null from disorders where dsm_chapter=5` — expect five Tier A rows, all linked. |

| `20260801_reject_drops_orphan_page_shell.sql` | Applied to `radlab-academic` 2026-08-01 by Claude via MCP `apply_migration` (name `reject_drops_orphan_page_shell`). Replaces `review_proposal()` and clears two rows. **Rejecting a proposal marked only the *version*, never the page it created** — `api/ingest.js` lands each returned page as a proposed version against a shell page (`content` null, status `proposed`), so rejecting the sole proposal for a new page left the shell behind permanently. Not merely untidy: `wiki_pages_bind_links()` resolves a `[[wikilink]]` by slug against **any** existing row, so an inbound link to a rejected page *resolved* — counted healthy in `wiki_links`, rendered blue in the reader, and led to a blank page. A red link is the honest state and the schema keeps unresolved links precisely so gaps stay visible; instead the corpus reported "0 red links" while carrying one link to nowhere (`what-is-abnormal` → `history-of-mental-illness`, a duplicate rejected 2026-07-31). Left unfixed this accrues **one pair per foundations run**, since proposing a duplicate of the target is exactly what those runs do. **Deletion, not archival**: `archive_page` is for a page that had a life and keeps its history and provenance; a shell whose only proposal was rejected never had an accepted body, and the model's full proposed text survives in `ingest_jobs.result_json`, which is where a rejected proposal's audit trail actually lives. Three guards, all verified in one rolled-back transaction under the instructor's JWT: a shell with a sole rejected `new` proposal **is** dropped; a page that already has a body is kept; a shell with a rival proposal still pending is kept (so a competing run is never pulled out from under the reviewer); and an `update` rejection is kept, since an update targets a page that predates the run. The reject branch now returns `page_dropped` and `links_orphaned` rather than deleting silently — stranded links revert to red via `ON DELETE SET NULL`. §2 removed the two shells already accrued (`history-of-mental-illness`, `research-methods-in-psychopathology`), both already at zero inbound links because the one live link was retargeted to `historical-traditions` through `edit_page()` first. Verify: `select count(*) from wiki_pages where content is null` (expect 0) and the blue-link-to-empty check — `select count(*) from wiki_links l join wiki_pages p on p.id=l.target_page_id where p.content is null` (expect 0). |

| `20260801_catalogue_substance_foundations.sql` | Applied to `radlab-academic` 2026-08-01 by Claude via MCP `apply_migration` (name `catalogue_substance_foundations`). Adds three `disorders` rows at **tier `foundation`, lecture 8, `dsm_chapter` null**. Module 11's paper run produced `substance-use-disorder` (10,233 chars — the longest page in the wiki), `substance-intoxication` (4,310) and `substance-withdrawal` (2,921), none with a catalogue row, leaving them reachable by link and search but invisible to browse-by-chapter and to `reference_worklist` — the state `internet-gaming-disorder` was in. **Neither the model nor the catalogue was wrong here**, and the distinction from earlier slug misses is worth keeping: `panic-disorder`/`agoraphobia` was a *grouping* split and `adjustment-disorder(s)` was *number*, but this is a **carve** mismatch. Module 11's outline runs 11.1.2 Substance Use Disorder → 11.1.3 Intoxication → 11.1.4 Withdrawal → 11.1.5 Types of Substances Abused (Depressants / Stimulants / Hallucinogens-Cannabis), so the source partitions by diagnostic pattern then by drug *class*, while the catalogue lists five specific substances. Paper mode carved the source as the source is built, which is what it is for; the two partitions simply do not align. **Foundation rather than Tier A or B** because DSM-5-TR defines all three constructs generically and applies them across substance classes — they are the scaffolding the five substance-specific Tier A pages will lean on, the role `suicide-and-self-harm` plays for the L6 block; Tier B would misrepresent load-bearing material as supporting detail. Idempotent (`NOT EXISTS` guard), resolves `course_id`/`page_id` by lookup rather than hardcoded UUIDs. **Catalogue 125 → 128 rows**; fall generated-page scope unchanged, since all three pages already exist as accepted drafts. **This does not close Lecture 8** — Tier A there remains 0 of 5. Four (alcohol, cannabis, opioid, stimulant) are recoverable from the same module by reference mode: the material is present but distributed (alcohol 52 mentions, cannabis 21, opioid 21, stimulant 19). The fifth is not — **gambling appears zero times in the entire module**, so `gambling-disorder` moves to the uncovered-topics list in run plan §4. Verify: `select slug, tier, page_id is not null from disorders where lecture=8 and tier='foundation'` (expect three rows, all linked) and `select count(*) from disorders` (expect 128). |

| `20260801_split_needs_from_annotations.sql` | Applied to `radlab-academic` 2026-08-01 by Claude via MCP `apply_migration` (name `split_needs_from_annotations`). **`needs` was conflating an empty section with a finished one carrying a caveat.** `extract_page_needs` scored a section as a gap on `marker OR not prose`, so a section holding only `> **Needs research:** …` and a section holding 3,900 characters of prose *plus* one line naming a sub-gap came out identical. Caught on the four Lecture 8 reference runs: of eleven flagged sections, **exactly one was truly empty** (`stimulant-use-disorder` → Contested); the other ten ran 1,764–3,980 chars with 4–15 prose lines. Consequence — `alcohol-use-disorder`, at 16,921 chars the most complete disorder page in the wiki, declared four gaps and sorted to the top of `reference_worklist`, which was offering the most finished page as the most-needing-work. Corpus-wide the split was **89 truly-empty sections across 52 pages vs 27 annotated across 17**, so about a quarter of every flag was not a hole. Adds `extract_page_sections(md) → jsonb` as a **single shared parser** (heading normalisation byte-identical to the old function, so values stay comparable) with two thin wrappers over it — `extract_page_needs` (no prose at all) and `extract_page_annotations` (has prose *and* a marker) — so the two definitions cannot drift. `wiki_gap_report` and `reference_worklist` both gain `annotations`, the latter also `annotation_count`; **new view columns are appended last** because `CREATE OR REPLACE VIEW` cannot reorder or rename existing ones (the first attempt failed trying to insert them before `reference_runs`). `security_invoker = true` preserved on both — without it they bypass the roster gate (fixed 2026-07-26). Backfill is scoped with `WHERE needs IS DISTINCT FROM …` so `updated_at` is not churned corpus-wide, and is safe against the six triggers on `wiki_pages`: `wiki_pages_snapshot` returns early when content/title/summary are unchanged, so **no spurious versions were minted (374 before, 374 after)**, and `sync_wiki_links` is idempotent. Effect: gap flags 104 → 77, pages with gaps 57 → 44, annotations 27 across 17 pages. The distinction now drives the two priority lists in run plan §8.6 — an **empty** section is missing template and is instructor work, an **annotated** section is a scoped ask ("this section says the cross-cultural data is missing") and is the best shape of student contribution task in the corpus. Verify: `select count(*) from reference_worklist where state='complete'` (expect 0) and `select slug, needs, annotations from wiki_gap_report where slug='alcohol-use-disorder'` (expect empty needs, four annotations). |

### Content edits applied through `edit_page()` (2026-07-30)

Not migrations — recorded here because they were applied from this session rather than through the portal, and because they are the first use of the new edit path. Both went through `edit_page()` under the instructor's JWT, so `updated_by`, the version snapshot and the change note are all set exactly as a browser edit would set them; the previous bodies survive as accepted versions.

- **`major-depressive-disorder`**: three copies of the six-section skeleton merged into one, **15,825 → 11,032 chars**, v3 → v4. Copy 3 was the spine and already contained copy 1's psychodynamic Treatment block verbatim plus an identical Contested section, so the third proposal had effectively already done the integration; copy 2 was a strictly poorer subset except for two facts, both kept (antipsychotics reserved for MDD with psychotic features; naming the monoamine hypothesis). Frontmatter rebuilt — the old `prevalence: Not addressed in this source` was false once the textbook content landed. Outbound links **1 → 21, all resolved**, by linking concepts the prose already named.
- **`persistent-depressive-disorder`**: two copies merged, 7,103 → 6,959 chars, v3 → v4. Copy 1 was the spine; copy 2 was genuinely additive in two places and both were folded in — the criteria structure (copy 1 described the category's *history* but never the symptom list) and the legacy DSM-IV prevalence and sex-ratio figures, which closed a gap copy 1 had explicitly declared. The Epidemiology marker was **narrowed rather than deleted**, since onset-age distribution and cross-cultural data are still genuinely absent. Outbound links 1 → 7, all resolved.

## Undated files in this directory

| File | Applied — evidence |
|---|---|
| `belt_correspondence_migration.sql` | live: `belt_trials.bt_baseline_period_ms`/`bt_condition_period_ms`, `belt_sessions.session_number`/`baseline_period_ms`/`post_baseline_period_ms` |
| `belt_mlr_migration.sql` | history: `belt_mlr_calibration_columns`; live: `belt_sessions.calib_model_label`/`calib_fit_r`/`calib_lag_ms`. (Duplicate of `src/games/BreathBelt/belt_mlr_migration.sql`.) |
| `questionnaires_schema.sql` | live: `questionnaires` table, trigger `questionnaires_updated_at`, lab-all + auth-read policies |

## SQL files outside this directory (`src/games/BreathBelt/`)

All applied: `belt_schema.sql` (live: `belt_sessions`, `belt_trials`), `belt_proportion_migration.sql`
(history: `belt_proportion_mag`; live: `belt_trials.proportion_mag`), `belt_sync_metrics_migration.sql`
(live: `belt_trials.belt_sync_mean`), `belt_mlr_migration.sql` (duplicate, see above).

## History entries with no repo file

These were applied directly (dashboard SQL editor / MCP) without a saved migration file — listed so
nobody hunts for missing files: `add_created_at_to_studies`, `studies_fk_on_delete_cascade`,
`grant_authenticated_access_all_tables`, `fix_schedule_instance_fk_set_null_on_delete`,
`stillwater_rls_authenticated`, `drift_rls_own_rows`, `add_reb_consent_forms`,
`consent_forms_storage_rls`, `fix_unsubscribe_tokens_study_fk_cascade`, `add_hair_columns_to_avatars`,
`fix_debrief_forms_rls_policy`, `add_debrief_forms_storage_policy`, `create_aptitude_suite_tables`,
`inperson_study_infrastructure`, `questionnaire_responses_lab_insert_policy`,
`stillwater_responses_user_id`, `stillwater_responses_rls_fix`, `audio_add_folder_and_file_meta`,
`drop_public_assets_broad_select_policy`.

Storage config (not schema, applied live via MCP `execute_sql` 2026-07-25): the `public-assets`
bucket's `allowed_mime_types` gained `audio/mpeg` (was image-types-only:
`image/png,jpeg,gif,webp,svg+xml`) so Tune's `.mp3` clips can be uploaded to `public-assets/tune-audio/`.
`file_size_limit` unchanged at 5 MB (every trimmed clip is well under). Image uploads unaffected.


---

## radlab-academic project (`qldgwpneygvgcvexlduz`) — separate from everything above

`20260805_wiki_provenance_readable_by_members.sql` — **applied live via MCP `apply_migration`
2026-08-05, verified.** Adds two additive `select` policies so course **members** (not just staff) can
read the rows behind `wiki_page_provenance`: accepted versions of **published** pages
(`wiki_page_versions`), and the `ingest_jobs` rows those versions cite. Drafts, pending/rejected
proposals and unrelated jobs remain staff-only; no existing policy was altered or dropped.

**Why:** the view is correctly `security_invoker=true`, but both underlying tables carried staff-only
read policies, so a student would have seen every page render with an **empty sources panel** — the
corpus's attribution claim invisible to the audience it exists for. Staff never hit it because staff
satisfy the staff policy.

**Verified 2026-08-05 by RLS test**, not by inspection: one page published, the TA enrolment
temporarily set to `student`, `SET LOCAL ROLE authenticated` with that user's JWT claims. Result —
1 page visible (0 drafts), provenance present with **5 sources**, and scoping correct: only **10**
versions and **5** jobs visible, i.e. exactly those belonging to the published page. Test state fully
reverted afterwards (role back to `ta`, page back to `draft`, 0 published, no leftover fixtures).

Same test established the **publish-all-at-once requirement**: with one page published, **all 13 of
its outbound links appeared broken to the student**, because `wiki_links` is member-readable while
unpublished targets are not.

`20260805_page_gaps.sql` — **applied live via MCP `apply_migration` 2026-08-05 (two calls: tables +
policies, then `populate_page_gaps()`), populated and verified.** Creates `page_gaps`, `gap_claims`,
the `open_gaps` view and `populate_page_gaps(course_id)`.

**The unit is the ask, not the annotation.** Measured before building: 301 `Needs research` markers,
averaging **2.39 distinct asks each** (215 of 301 contain more than one, written as "X; Y; and Z").
One row per annotation gives a pool of 301 — under half of what 200 students × 3 contributions needs.
Split on asks it is **675**, plus **62** empty sections = **737 gaps**.

Identity survives re-parsing via `(page_id, ask_hash)`, md5 of the normalised ask, so a claim is not
lost when neighbouring prose is edited. `populate_page_gaps()` is idempotent: re-running refreshes
`last_seen_at`, inserts new asks, and **never deletes** (that would orphan claims) — disappeared asks
surface in `stale_gaps` for staff to retire.

RLS: gaps follow page visibility (`members read gaps on published pages`), so a student is never
offered work on a page they cannot read; students see and write only their own claims.

**Difficulty is auto-seeded and needs a human pass — it is a triage aid, not a classification.** The
regex produced false positives in both directions: "the opioid crisis" matched the crisis-line rule
(red → green), "hours-per-week dose-response" and receptor pharmacology matched drug dosing
(red → amber), while "Canadian reporting duties" was let through as green when mandated reporting is a
legal standard (green → red). Those four rules were corrected in place; the residue is unaudited.
Final seed: **green 134, amber 597, red 6**.

Capacity: green gaps set to **2**, since prevalence and rates asks legitimately take more than one
source (different populations, criteria, years). **Student slots 865** against 600 required, and
**green slots 268** against 200 students needing a scaffolded first task.

`gap_review_queue` view — **applied live 2026-08-06.** Surfaces only the *boundary* cases in the
auto-seeded `page_gaps.difficulty` classification, so reviewing it is a **17-row job rather than a
737-row one**. Rows with a null `review_reason` need no attention.

```sql
select slug, difficulty, ask, review_reason
from gap_review_queue where review_reason is not null
order by review_reason, slug;
```

Four reasons, in priority order — the first three are the direction that matters:

| review_reason | what it catches |
|---|---|
| `MAYBE RED: reads as clinical instruction` | dosing, taper, deprescribing, antidote, overdose, withdrawal management |
| `MAYBE RED: legal or forensic standard` | statute, sentencing, civil commitment, criminal responsibility, duty to warn, forensic use/criteria/status |
| `MAYBE RED: crisis resource or coercive practice` | crisis line, helpline, safety plan, restraint, seclusion |
| `MAYBE NOT GREEN: conceptual, not a lookup` | mechanism, why, whether, debate, critique, theory |
| `MAYBE NOT RED: no clinical or legal trigger` | over-flagged red, wasting capacity |

**Regex caution recorded in the view comment: do not use `\yAct\y`** — it matches **Acceptance and
Commitment Therapy** and produced four false positives on the first pass. The word "capacity" likewise
matches "the capacity for temporary suppression" on `tic-disorders`, and "crisis" matches "the opioid
crisis". Anchor legal terms to their neighbours rather than matching them alone.

**Review pass 2026-08-06:** 5 rows reclassified **to red** (the conservative direction) —
`tarasoff-duty-to-warn` (what a warning must contain), `intermittent-explosive-disorder` (criminal
responsibility), `parasomnias` and `sleep-wake-disorders` (forensic sleep-medicine criteria and the
sleepwalking defence), `psychopharmacology` (deprescribing), `suicide-and-self-harm` (involuntary
hospitalisation). Final: **green 134 / 268 slots, amber 592, red 11.** Remaining to review: 11
`MAYBE NOT GREEN` and 1 `MAYBE NOT RED`, neither of which is a safety question.

`20260806_gap_submission_precheck.sql` — **applied live 2026-08-06; file written and re-applied with
two regex fixes later the same day.** Adds submission fields to `gap_claims` (`submitted_text`,
`source_doi`, `source_url`, `limitation`, `precheck`, `precheck_at`), the functions
`precheck_submission()` / `run_precheck()`, and the `submission_review_queue` / `gap_review_queue`
views.

> **This ran for a day with no migration file.** The objects were created live via MCP; the file was
> written afterwards by transcribing `pg_get_functiondef()` / `pg_get_viewdef()` back out of the
> database. Transcribing them is what exposed the two bugs below — reading the stored source is a
> cheaper audit than it looks.

**Why it exists.** At 200 students × 3 contributions the irreducible human step — opening the source
and confirming it says what the student claims — runs to **50–100 hours a term**. The precheck removes
the mechanical failures before a human reads anything, so that time goes to the judgment that cannot
be automated.

**Nine checks**, in the order they fire:

| code | severity | catches |
|---|---|---|
| `red_gap` | block | a red-classified gap reaching the student path at all |
| `no_identifier` | block | citation with no DOI or URL — unresolvable, therefore uncheckable |
| `doi_malformed` | warn | DOI that is not shaped like a DOI |
| `clinical_instruction` | block | **the likeliest novice failure** — dose, mg, titrate, taper, "first-line", "should be given" |
| `legal_assertion` | warn | "is required by law", "must report", "is a criminal offence" |
| `quote_too_long` / `quote_long` | block / warn | fair dealing — longest quoted run over 300 / 150 chars |
| `too_short` / `too_long` | block / warn | under 60 words / over 400 |
| `no_limitation` / `thin_limitation` | block / warn | the "what this source cannot tell us" field |
| `duplicate_source` / `duplicate_claim_source` | warn / block | DOI already cited on the page, or already used by another submission for the same gap |

**`route` tells a TA where to spend attention**: `BLOCKED` needs no reading at all (send back),
`light check` is a green gap that passed (confirm the number matches), `full read` is everything else.

**Clickable review links.** `review_url` resolves to the exact section a gap sits in —
`/academic/fieldguide/wiki/<slug>#<section>`. Section was backfilled by locating each ask inside the
`## `-delimited chunks of page content: **666 of 737 gaps anchor to a section**, the remaining 71 link
to the page top. `review_url_full` is the absolute form.

### Two bugs, found by transcribing the function rather than by testing it

Neither had reached a student — `gap_claims` was still empty — but both would have fired on day one.

**1. The word count never worked.** The split pattern was written `'\\s+'`, which in a
standard-conforming string is an escaped literal backslash followed by `s`. It matched nothing, so
`regexp_split_to_array` returned **one element for any input**. Every submission would have been
blocked with `too_short: 1 words. Minimum 60.` — the precheck would have rejected the entire class —
and every one would also have warned `thin_limitation`, since `1 < 8`.

**2. Five of the ten `clinical_instruction` patterns were dead.** They used `\b` as a word boundary.
**In Postgres ARE `\b` is the backspace character**; the word boundary is `\y`. So `\d+\s?mg\b`,
`\bdosage\b`, `\bdosing\b`, `\btitrat` and `\btaper\b` could never match. Only the plain-text
alternatives (`first-line treatment`, `patients should`, …) were live — which is why the original
fixture appeared to work.

**Why the original fixture test missed both.** The bad submission raised its `clinical_instruction`
finding on `first-line treatment`, a *surviving* alternative, so the four dead patterns behind it went
unnoticed. And the good submission raised `too_short`, which was recorded here as *"59 words against a
60-word floor"* — a boundary condition, entirely plausible. **It was not 59. It was 1.** The check
fired for a believable reason and a wrong one at the same time.

> **Lesson worth carrying: assert on the value in the finding, not on the finding's presence.** A
> check that fires where you expected it to fire tells you almost nothing — `too_short` was *correct
> output from broken logic*. Reading `1 words` instead of `59 words` in the detail string would have
> caught it a day earlier.

**Re-verified after the fix**, with the same two-fixture shape: a well-formed submission (~90 words,
valid DOI, real limitation) now returns `[]` — **a clean pass, which was previously impossible** — and
one ending "Start with 20 mg daily and titrate upward" is correctly blocked as `clinical_instruction`,
which it previously was not. Both fixtures deleted; `gap_claims` back to 0.

Note `gap_claims` carries a unique constraint on `(gap_id, person_id)` — one claim per student per gap.
Fixtures must therefore target two different gaps.

**What precheck cannot do**, stated in the view comment so nobody assumes otherwise: confirm the source
actually says what the student claims. That remains the human step, and it is the whole job.

`20260807_course_structure.sql` — **applied via MCP `apply_migration` 2026-08-07, verified by count
and unmapped-drafts check.** The 2026F calendar as data, and the page→lecture mapping that gives
students a by-week axis into the Field Guide (WP6 Phase A; taxonomy §2a is the prose record).

- **`course_structure`** — 14 rows: 11 content lectures, the Oct 14 midterm (+ RCT onboarding), the
  Oct 28 reading week, the exam-period row. `week_no` (1–14, calendar) and `lecture_no` (1–11,
  content) **diverge after the midterm** — L6 is week 7; both are stored, neither derived. RCT arc
  and deadline notes live in `note`.
- **`page_lectures`** — 266 rows mapping **259 of 260 draft pages** (verified: the single unmapped
  draft is `elimination-disorders`, out-of-scope by design). Seven pages double-mapped for
  taught-in-both reasons recorded inline (tics L3+L9, RAD/DSED L4+L9, schizotypal PD L10+L11,
  psychodynamic psychotherapy L1+L2, Little Albert L1+L3, student support L1+L5). Inserted by slug
  join against `wiki_pages` — a typo'd slug drops the row rather than erroring, which is why the
  verification step counts uniques against drafts.
- **`gaps_by_lecture`** — `security_invoker=true` view feeding the Phase B student browser. Verified:
  every lecture carries 19–121 open gaps; per-lecture totals sum above 737 because double-mapped
  pages' gaps appear under both lectures, which is intended (a gap findable from either week).
- RLS per the standard pattern: members read, staff manage, both tables.

Planning fact recorded here because the browser copy depends on it: **pre-midterm lectures (L1–5)
hold only 38 green gaps = 76 slots** against ~200 students needing a green first task by Oct 7 — the
green deadline must allow claiming from any lecture, not only material taught so far.

`20260807_gap_board.sql` — **applied via MCP `apply_migration` 2026-08-07, verified under two JWTs.**
One function, `gap_board()`: the single read behind the student gap browser
(`/academic/fieldguide/gaps`, WP6 Phase B).

**Why SECURITY DEFINER and not a view.** Remaining capacity = capacity minus *all* students' active
claims, but `gap_claims` RLS correctly lets a student read only their own rows. A `security_invoker`
view would silently under-count — the nested-RLS failure mode again (it returns too little, it does
not error) — and a definer *view* is against house rules. So: a definer function gated inside by
`is_course_member()`, returning **counts only, never names**. `my_status` is the caller's own claim
state, which is theirs to see. `withdrawn` is excluded from the active count everywhere, which is
also how the future 14-day claim TTL will free abandoned holds without this function changing.

**Verified**, not inspected: as norman.farb (member) → **760 rows, 11 lectures, 268 green slots
open**; as an authenticated non-member UUID → **0 rows**.

Rows are 760 rather than 737 because double-mapped pages surface their gaps under both lectures —
intended: a gap should be findable from either week it is taught in.

`20260808_gap_triage_adjudicated.sql` — **applied 2026-08-08 (as `…adjudicated` + `…adjudicated_p2`
on the server; one file here because it is one decision), verified: `gap_review_queue` flags 13 → 0.**
Norm adjudicated every flagged row, accepting all recommendations.

- **10 greens demoted to amber** — multi-part/conceptual asks a light-check TA cannot verify against
  one figure. **Capacity 2 retained** on all ten (Norm's call): capacity means *how many independent
  contributions the ask can absorb*, orthogonal to difficulty — a green at 2 collects **convergent**
  evidence (two sources, same lookup; `duplicate_claim_source` already forces the second student onto
  a different source), a multi-part amber at 2 collects **complementary** evidence (one facet each).
- **1 green kept** — `vascular-neurocognitive-disorder` ("does midlife BP lowering reduce dementia
  incidence"): reads conceptual, but SPRINT MIND and successor meta-analyses answer it from one
  source. Marked `adjudicated: keep green` in notes.
- **Both reds kept** — the two MAYBE NOT RED flags were regex blind spots, not misclassifications:
  "Canadian **reporting duties**" is mandatory-reporting law and "**involuntary hospitalisation**"
  is civil-commitment territory. Both phrases added to the view's not-red trigger list.
- **Adjudication override added to the view**: `notes ~* 'adjudicated: keep'` returns NULL before any
  heuristic fires, so a settled human decision is never re-flagged. Anyone re-triaging later: mark
  the row, don't fight the regex.

Board after: **green 124 gaps / 248 slots** (headroom 1.24× for ~200 students' first task),
**amber 602 / 612**, red 11, total slots unchanged at 860. Demotions are audit-trailed in
`page_gaps.notes`.

`20260808_claim_flow.sql` — **applied via MCP `apply_migration` 2026-08-08, verified by a full
student-lifecycle test battery under a throwaway student account (created via a real `auth.users`
insert so `handle_new_user` ran, deleted after).** WP6 Phase C, server side: claiming, drafting,
submitting. Every rule Norm decided is enforced in the database, not the client.

**Two live security holes this closes** (found by reading the RLS policies before building):
`members update own claims` let a student set `status='accepted'` on their own row — self-grading —
or overwrite `precheck` with `[]`; `members create own claims` let a student INSERT directly,
bypassing capacity, green-first, and the red block. Three-layer fix:

1. **Column grants**: `UPDATE` on `gap_claims` revoked and re-granted on a named column list —
   `precheck`/`precheck_at` are now writable only by definer functions running as table owner.
   (Verified: student UPDATE of precheck → `42501 permission denied` before RLS is even consulted.)
2. **Guard trigger** (`gap_claims_guard`, BEFORE INSERT OR UPDATE, SECURITY DEFINER so its
   page_gaps read survives student RLS): staff pass untouched (SubmissionsQueue accept/send-back
   unaffected); non-staff inserts and withdrawn→claimed revivals require the transaction-local
   `radlab.claim_flow` flag that only `claim_gap()` sets; claimed→submitted requires the flag that
   only `submit_claim()` sets, **so the precheck is unskippable**; submitted and accepted rows are
   locked to students entirely.
3. **Definer functions as the only doors**: `claim_gap()` (red block → capacity live-count →
   green-first both halves: first claim must be green AND amber locked until a green is
   submitted/accepted → max 2 unsubmitted claims → insert-or-revive with `expires_at = now()+14d`);
   `submit_claim()` (runs `precheck_submission` FIRST — any `block` finding records the precheck and
   leaves the claim in `claimed`, so a fault costs an edit, not the claim); `check_doi(gap, doi)`
   (the immediate check: malformed / already-cited-on-page / already-used-for-gap, run on paste);
   `gap_page_sources(gap)` (what the page already cites, shown at the moment of choosing);
   `expire_claims()` (hygiene sweep, staff-scoped — counts already ignore expired rows, so nothing
   breaks if it never runs).

`gap_board()` regained with three new columns (`my_claim_id`, `my_expires_at`, and expiry-aware
`claims_active`/`remaining`).

**Test battery, all passed**: amber-first → `green_first`; red → `red_gap`; green claim → ok with
+14d expiry; duplicate claim → `already_claimed`; direct INSERT → trigger raise; self-accept →
trigger raise; precheck forgery → column grant denial; direct `status='submitted'` → trigger raise
naming `submit_claim()`; submit with "Clinicians should titrate stimulants starting at 20 mg daily"
→ `blocked` (clinical_instruction — the `\y` fix earning its keep), claim stayed `claimed`; clean
resubmit → ok, `[]` findings; post-submit edit → locked; two ambers claimed after green submitted,
third → `too_many_open`; withdraw → slot freed in `gap_board` immediately; `check_doi` under a
*different* person's JWT → `duplicate_claim_source` block on the used DOI, warn on malformed, `[]`
on clean. Fixtures fully deleted (claims 0, fixture people 0).

`20260810_correction_guards.sql` — **applied 2026-08-10 (one message-format fix re-applied in the
same session: RAISE placeholders are bare `%`, not `%s`), verified by exercising every guard path
live.** The staff correction path (WP6 Phase D, first half).

**The design discovery:** `edit_page()` already *was* the correction path — staff-gated, snapshot
trigger keeping numbered history, note attached to the version row, and `job_id IS NULL` so a staff
edit can never appear in a page's "Built from" list. This migration hardens that existing door
instead of cutting a second one. The governing rule (run plan §38 Phase D): a correction changes
*how* the page says something, never *what it claims* — changed claims need a source, i.e. the
ingest path.

Decisions (Norm, 2026-08-10): **auto-apply + audit feed**, **quiet visibility**, **magnitude
tripwire** to his spec ("11%→12% shouldn't matter; →21% or →3% should; half an order of magnitude").

- **`numeric_shift_check(old, new)`** — pairs vanished numbers with appeared numbers in document
  order; per pair: |Δ|≤3 passes ("a few points of variance"), then trips at ratio ≥√10 (~3.16×) OR
  |Δ|≥10 (11→21 is a different claim at only 1.9×). Years 1900–2099 excluded (citation edits);
  unpaired numbers reported but never trip — whether a *new* number is a new claim is the form's
  question, not a regex's. Verified against all eight spec cases including n=1234→134 (trip) and
  0.5→1.9 (pass).
- **`edit_page()` re-created with guards** (old 3-arg signature **dropped** — two overloads would
  make every named-arg PostgREST call ambiguous): note now REQUIRED; section-list guard (removing a
  `##` heading blocks without the explicit `p_allow_structure` override — the `## Contested` lesson
  encoded); annotation-removal guard (a `> **Needs research:**` line is a catalogued gap); the
  tripwire (block until `p_verified` is supplied, which is then appended to the note as
  "verified against source: …").
- **`corrections_feed`** — security_invoker view over job-less accepted versions with notes;
  **retroactively covers the WP-era staff edits back to 2026-08-02**, notes intact.

Client: WikiPage's editor reveals the matching remedy on refusal (verified-input on NUMERIC SHIFT,
override checkbox on STRUCTURE), save disabled without a note; `/academic/fieldguide/corrections`
lists the feed; the home page gains a Corrections card with a this-week count.

**Live verification** on `little-albert-study`: no-note → refused; 40→12 without verification →
refused listing `40→12 (Δ28, ×3.33)`; same edit with a verification statement → accepted, note
carries it; section removal → refused naming the section; final edit restored the page
**byte-identical** to its pre-test body (proven by comparing against the v2 version row).

`20260811_wp7_curated_gaps.sql` — **applied 2026-08-11 via MCP `apply_migration` (name
`20260811_wp7_curated_gaps`), verified by reading the six rows back and re-counting the corpus.**
Data only, no DDL. Six `kind='curated'` gaps from the WP7 item-format audit (run plan §39.10): exam
questions were written against real guide pages and the locked study text could not answer them.

**Why they had to be curated rather than derived:** all six sit in sections that already contain
prose, so `extract_page_needs()` reads them as filled and `populate_page_gaps()` can never produce
them. This is the §5 annotated-vs-derived problem in a third form — a section can be full and still
be missing the thing the chapter depends on.

**First use of `kind='curated'`** (the check constraint allowed it; 0 rows before). It is the
staff-flagging route from run plan §38.3 Phase D: recorded against the page without touching page
content, so no flag passes through provenance. `populate_page_gaps()` is unaffected — it only writes
`annotation` and `empty_section` rows and never deletes — and `ask_hash` follows the function's own
convention for prose asks, `md5(lower(ask))`, so the unique index behaves. Idempotent via
`ON CONFLICT (page_id, ask_hash) DO NOTHING`.

Rows: `schizophrenia`/etiology — the dopamine hypothesis (green, the mechanism is assumed
chapter-wide and stated nowhere) and longitudinal brain change (amber);
`positive-and-negative-symptoms` — how the distinction maps onto prodromal/active/residual (amber;
the corpus currently implies the *opposite* of the usual teaching claim); `brain-reward-system` —
neuroadaptation and wanting-vs-liking (green) and human evidence for the reward account (amber);
`law-and-ethics` — duty where the risk is to the patient rather than a third party (**red**: Tarasoff
and Smith v. Jones are both third-party doctrines, this is provincial statute, and the corpus has
been wrong about a statutory threshold once already).

Corpus after: **743 open gaps · 126 green · 605 amber · 12 red · 867 student slots** (from 737/860).
All four pages are lecture-mapped (8, 10, 11), so the rows are reachable in the student gap browser.

`20260806_staff_read_enrolled_people.sql` — **applied live 2026-08-06 (three MCP calls as each fault
revealed the next), verified by RLS test.** Fixes "permission denied for table people" on
`/academic/fieldguide/submissions`, introduced the same day by `submission_review_queue` joining
`identity.people` to show whose submission a row is.

**Three faults, each hidden behind the one in front of it:**

1. **Grant.** `identity.people` had SELECT granted to `postgres` only, so client queries failed on
   privileges *before* RLS was consulted — hence "permission denied" rather than an empty result. The
   pre-existing `read own person row` policy had **never been reachable from the client**; nothing had
   queried the table until now.
2. **Schema usage.** The table grant is unreachable without `usage on schema identity`. USAGE confers
   nothing by itself — every table there still needs its own grant plus RLS, and only `people` has one.
3. **Nested RLS.** The first staff policy tested `enrollments` directly, but that table carries
   `read own enrollments`, so the EXISTS subquery could only ever see the caller's own row. **A
   reviewer saw exactly one name — their own — and every submission would have rendered with a blank
   student.** Fixed with `shares_staffed_course()`, SECURITY DEFINER, for the same reason
   `is_course_member` / `is_course_staff` are: an authorisation check must see rows the caller cannot.

**Verified with `SET LOCAL ROLE authenticated`**, not by inspection: instructor → **2 rows** (self plus
the TA in the same course); student → **1 row** (self only). Test enrolment flip reverted.

**Lesson worth carrying:** a policy whose predicate reads another RLS-protected table silently
under-matches. It does not error — it returns too little, which looks like missing data rather than a
permissions bug. Any new policy that joins `enrollments` should go through a definer helper.

---

`20260811_workbench_sessions.sql` — **applied 2026-08-11 via MCP `apply_migration` (name
`workbench_sessions`), visibility verified live under `SET LOCAL ROLE authenticated` against three
real accounts.** The Workbench: sharing Claude Code sessions with lab members (website.md §32).

Creates `claude_sessions`, `claude_session_turns`, `claude_session_shares`, `claude_project_shares`,
the `can_view_claude_session(uuid)` visibility helper, and `workbench_shareable_members()`.

**The gate that matters is not the role.** `profiles.role = 'lab'` already grants `/admin/*`, so every
student in the lab holds it — gating the Workbench on lab role would have shown all of Norm's captured
sessions to all of them while looking like access control. The share tables are the entire access
story, and both guards (`WorkbenchRoute`, `WorkbenchAdminRoute`) are written on that assumption.

`can_view_claude_session()` is SECURITY DEFINER for the reason `20260806_staff_read_enrolled_people`
and `20260807_gap_board` both record: it resolves `project_key` by reading `claude_sessions`, which is
the table being policed, so an invoker-rights version would recurse or silently return nothing.

**Verified 2026-08-11** with a seeded real session (8 turns), impersonating Norm (super admin), Jaafar
and sandy (both `role='lab'`, neither super admin), counts captured to a scratch table because MCP
does not surface `RAISE NOTICE`:

| Probe | Result |
|---|---|
| Before any share — Jaafar | **0** sessions, **0** turns |
| Session shared with Jaafar — Jaafar | **1** session, **8** turns |
| Same moment — sandy (unshared, also `role='lab'`) | **0** sessions, **0** turns |
| Norm, holding no share row at all | **1** session (super-admin policy) |
| Standing project rule added for sandy | **1** session, **8** turns — no per-session row needed |
| sandy reading `claude_session_shares` | **0** rows — a member cannot learn who else holds a session |
| Jaafar inserting his own share row | **blocked, SQLSTATE 42501** |

Probe table dropped and both test share rows deleted afterwards; the seeded session remains, shared
with nobody.
